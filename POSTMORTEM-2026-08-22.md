# POSTMORTEM — คดี "เข้า AR แล้วออกมาโหมดอื่นขยับไม่ได้" (22 ส.ค. 2026)

**ระยะเวลา:** 1 วันเต็ม · **8 รอบวิเคราะห์ · 6 บิลด์ (9018-9023) · โควตา EAS 6/15**
**ปิดคดี:** user ยืนยันบน build **9023 (framework b465)** — "ใช้ได้แล้ว"
**บทเรียนฉบับย่อสำหรับ session อื่น:** memory `feedback_on_screen_telemetry_for_device_bugs`

---

## ต้นเหตุจริง (เรียงตามผลกระทบ)

### 1. 👻 กล้องผี — ตัวการหลักที่หลอกทั้งคดี (รอบ 8, `0b98a1f`)
ทางแก้ jetsam ของ 16 ส.ค. คือ "ปล่อยฉากทั้งก้อนตอนพักแอป" ⇒ `AppBoot.Start()` **รันซ้ำได้
ทั้งชีวิตแอป** · ถ้าจังหวะนั้นกล้องเดิมแขวนอยู่ใต้ริก AR (`ArKitSession` เป็น DontDestroyOnLoad
— ของใต้มันรอดการโหลดฉาก) `Camera.main` จะหาไม่เจอ → `SetupCamera` **คลอดกล้องตัวที่สองซ้อน**
→ ตัวเก่ากลายเป็นผี แช่ pose สุดท้าย (จุด warp ใต้ท้องเรือ) **วาดทับทุกอย่างตลอดกาล**

อาการที่ตาเห็น: "เกมค้าง ขยับอะไรไม่ได้" — ความจริง: เกม/จอย/โดรน/กล้องหลัก**ทำงานปกติทุกระบบ**
เราแค่มองโลกผ่านตาผี · ภาพ "ค้างใต้เรือ" ทุกใบทั้งวันคือผีตัวเดียวกัน

แก้: `SetupCamera` ตามหากล้องเดิม (รวม inactive) แล้วชุบคืนก่อนคิดจะสร้างใหม่ +
ตาข่ายถาวรใน `UiShell.Update`: **กล้องที่วาดลงจอ (targetTexture ว่าง) มีได้ตัวเดียว** ผีทุก
สายพันธุ์ถูกปิด+log ชื่อ

### 2. คำสั่งโหมดถูกทิ้งเงียบเมื่อแมพเดิม (รอบ 3, `9ba6377` — b451)
`SwitchMapFromHost` return ทันทีเมื่อ shortId เดิม ⇒ `EnterHostMode` (ที่เดียวที่อ่านคำสั่งโหมด)
อยู่ในเส้นโหลดแมพ = ไม่มีใครอ่านคำสั่งเลย · Unity ค้างโหมดเก่า แอปวาด chrome โหมดใหม่ทับ
แก้: `SyncHostMode` — สาขาแมพเดิม + payload ที่มีแต่ mode (จาก `mode:"view"` ตอนออกจอ)

### 3. AR รอบสองจอดำ — deferred-destroy (รอบ 5+6, `d1beda5`+`318ac7a`)
`Destroy()` มีผล**ปลายเฟรม** · การออก-เข้า AR ในเฟรมเดียว (เข้าซ้ำแมพเดิม / คิว message
view+ar ติดกัน) ทำให้ `GetComponent` เจอ**ซากที่กำลังจะตาย** → ไม่สร้างตัวใหม่ → รอบสอง
ไม่มี ARCameraManager/Background/PoseDriver/**ARSession** = จอดำ ขณะ plane detection
(อยู่บน XROrigin ที่สร้างใหม่ทุกรอบ) ยังทำงาน — ลายเซ็น "เจอพื้นแล้ว บนจอดำ"
แก้: ปิดเก็บ+ปลุกด้วย disable→enable ทั้งสี่ตัว (สูตรเดียวกับ ARInputManager ที่คอมเมนต์
บันทึกบทเรียนไว้แล้วแต่แก้ไม่ครบชุด) + เลิก `StopSubsystems` ซ้อน component (สองผู้จัดการ
สั่งเครื่องเดียวกัน = สถานะที่ไม่มีใครรู้จัก — ราก "state ค้าง SessionInitializing")

### 4. จุดเกิดโดรนตายตัวใต้ท้องเรือ (รอบ 5+6, `33aea19`+`318ac7a`)
- แมพ Chang มี **ประตูวาป 2 อัน** → กฎ E8 (เกิดข้างประตู) **ชนะการสุ่มเสมอ** ⇒ จุดเกิดตายตัว
  และด่านตรวจที่เพิ่มให้เส้นสุ่ม (D9) ไม่เคยถูกใช้เลย
- `Settle` เช็ค "นิ่ง" — แต่จุดที่ถูกดันสองทางสมดุล (ท้องเรือกด+ทรายดัน) ก็นิ่ง ⇒ ปล่อยผ่าน
  แล้ว `Resolve` หักล้างทุกคำสั่งจอยทุกเฟรม = โดรนแช่แข็งจริง (คนละเรื่องกับกล้องผี แต่อาการ
  กลืนกัน)
แก้: `SpawnIsClear` (เกณฑ์เดียวกับ Resolve เป๊ะ — "นิ่ง" ≠ "ว่าง") + จับสลาก 8 ใบ + fallback
จุดเปิดแมพ + **ประตูวาปที่จุดลงไม่ว่างจริงถูกปฏิเสธ** ตกไปเส้นสุ่มที่มีด่านครบ

### 5. ปลาไม่ตกใจตอนพุ่งชน (รอบ 6.5, `846e65d`)
`PushOutOfAnimals` ดันโดรนออกจากตัวสัตว์แล้ว**ตั้งความเร็ว = 0 ก่อน** `SetDiver` รายงาน
⇒ วินาทีที่ชน สัตว์เห็นความเร็ว 0 เสมอ — "พุ่งเต็มสปีด" กับ "ลอยเบียด" แยกไม่ออกโดยโครงสร้าง
แก้: อ่านความเร็วขาเข้าเก็บไว้ก่อนการดัน แล้วรายงานตัวนั้น

### 6. บั๊กจริงอื่น ๆ ที่แก้ระหว่างทาง (ทุกตัวจริง แม้ไม่ใช่ "ตัวการ")
- สัตว์เดี่ยว (ฉลามวาฬ) ไม่เคยได้ระยะ/แรงตกใจแบบไล่ระดับของ 21 ส.ค. → `DiverStartleRadius`+`DiverFleeSprint`
- โดรนหันเร็วไป → YawRate 63→49°/วิ · ขึ้น-ลง 1.44→1.10 m/s
- AR มีการ์ดสัตว์เด้ง → `ShowsInfoCard` ยกเว้น Ar — **และพบว่ากฎตัวนั้นไม่มีใครเรียกจริง** ต้องต่อสายที่ `InfoCardController` ด้วย
- ปลากะมงตั้งฉากกับพื้น → pod เดินสาขา `LookRotation(vel)` (เงยตามความเร็วแนวดิ่ง ≤29°)
  ที่การแก้ build 244 ไม่ครอบ → ย้าย pod มา slot formation + ถุงรูปฝูงของตัวเอง (ตัดทรงเสา)
- `UiShell` ปิด OrbitCamera ทั้ง component เพราะนิ้วบน UI — touch ผีปักกล้องค้างถาวรได้
  → ย้ายกฎไปเช็คตอนเริ่ม gesture รายนิ้วใน OrbitCamera

---

## ความผิดพลาดของกระบวนการ (สิ่งที่ทำให้ 1 บั๊กกลายเป็น 8 รอบ)

| # | ความผิดพลาด | ราคา | ทางแก้ที่ใช้แล้วได้ผล |
|---|---|---|---|
| 1 | **อนุมานจากภาพนิ่ง 5 รอบติด** ทั้งที่ทุกทฤษฎีพิสูจน์ไม่ได้ — และจอที่ใช้เป็นหลักฐานคือ "จอโกหก" (วาดโดยกล้องผี) | ~4 บิลด์ + รอบเทส user | telemetry ขึ้นจอ: bNNN → โหมด/orbit → log tail → สุขภาพกล้อง — **ออกแบบให้จับความขัดแย้ง** (cam ถูกทุกหลัก+จอผิด ⇒ บังคับข้อสรุปเดียว) |
| 2 | แยกไม่ได้ว่าภาพ user มาจากบิลด์ไหน (9017/9018 จอเหมือนกัน) | เผารอบเทสฟรี + วิเคราะห์บนตัวเก่า | ป้ายเลขบิลด์บนจอ — ภาพทุกใบพิสูจน์ตัวเอง |
| 3 | แก้ "สิ่งที่สมเหตุผล" โดยไม่มีหลักฐานตรง (RoutesViaHub, orbit snapshot, ฯลฯ) — ทุกตัวเป็นบั๊กจริงแต่ไม่ใช่ตัวการ อาการเลย "ไม่หาย" ซ้ำ ๆ | ความเชื่อมั่นของ user | กฎ FISH_TUNING ที่มีอยู่แล้ว: "รู้ให้ได้ก่อนว่าอาการอยู่ระบบไหน" — และเมื่อครบ 2 รอบไม่มีข้อมูลใหม่ = หยุดเดา สร้างเครื่องมือ |
| 4 | เข้าใจสถาปัตยกรรมผิด: คิดว่า chrome จอยเป็นของ RN → ไล่ทาง touch ผิดระบบ | 1 รอบวิเคราะห์ | เปิดโค้ดยืนยันก่อนตั้งทฤษฎี (จอยคือ uGUI ของ Unity) |
| 5 | แก้บทเรียนเก่าไม่ครบชุด: deferred-destroy ถูกบันทึกไว้แล้วกับ ARSession/ARInputManager แต่สามตัวบนกล้องไม่เคยได้รับการแก้เดียวกัน — แล้วผมก็ซ้ำรอย: แก้สามตัวลูกแต่ลืมตัวแม่อีกหนึ่งรอบ | 2 บิลด์ | เจอ pattern-bug หนึ่งจุด = grep หาพี่น้องร่วม pattern ให้หมดก่อนปิดงาน |
| 6 | ไม่เช็คข้อมูลจริงของแมพจนรอบ 6 — "depth 20.0 เป๊ะทุกครั้ง" คือลายเซ็นของจุดตายตัว (ประตูวาป) ที่การสุ่มอธิบายไม่ได้ แต่ปล่อยผ่านไป 2 รอบ | 2 บิลด์ | ตัวเลขที่ "นิ่งผิดปกติ" = หลักฐาน ไม่ใช่เรื่องบังเอิญ · ดึงข้อมูลจริง (API แมพ) มาประกอบทุกครั้ง |
| 7 | สร้างเส้นทางพังใหม่ด้วยมือตัวเอง: `mode:"view"` (รอบ 4) ทำให้ Exit→Enter เกิดในเฟรมเดียว = จุดชนวน deferred-destroy | 1 บิลด์ | ทุก fix ที่เพิ่ม "ลำดับเหตุการณ์ใหม่" ต้องไล่ผลข้างเคียงกับกับดักที่บันทึกไว้แล้วในรีโป |

## สิ่งที่ทำถูกและควรทำซ้ำ

- **CI ฟรี ยิงได้ไม่ต้องเสียดาย · EAS มีราคา รอ user เคาะเสมอ** — คดีนี้ CI 6 รอบ EAS 6 รอบ
- sha256 ตรวจจาก CDN จริงทุกครั้งก่อนปัก framework — ไม่มีรอบไหนพังจากไฟล์ผิด
- เทส harness + `EditMode tests` บน CI เขียวก่อนทุกบิลด์ — ไม่มี regression หลุดสักรอบ
- ตาข่าย (net) ควบคู่การแก้ราก: กล้องผีมีทั้ง "แก้ต้นกำเนิด" และ "ด่านถาวรที่ฆ่าทุกสายพันธุ์"
- เครื่องมือทั้งชุดยังอยู่ในโค้ด ปิดด้วยสวิตช์เดียว (`badge: 1` ใน boot payload ของแอป) —
  บั๊ก device-only รอบหน้าเปิดคืนได้ใน 1 บรรทัด + OTA

## สถานะปิดคดี

- build **9023** (b465) บน TestFlight — user ยืนยัน "ใช้ได้แล้ว"
- badge/log ถอดออกแล้วผ่าน OTA (group `998fff27`) ตามคำสั่ง user
- Android: พักตามคำสั่ง user — เช็คลิสต์ค้างอยู่ใน `siamdive-rn/HANDOVER-APP.md`
- โควตา iOS ส.ค.: ใช้ 8/15
