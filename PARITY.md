# PARITY — ฟีเจอร์เว็บ (maps.siamdive.com/builder.html) เทียบแอป Unity DiveMap

> เขียน 2026-07-30 เพราะรายการ "งานที่เหลือ" ที่เคยรายงานไว้ **นับตาม WO ใน DESIGN_DOC เท่านั้น**
> แล้ว WO ไม่ได้ครอบคลุมฟีเจอร์จริงของเว็บทั้งหมด (ทัวร์โดรน/เกมเก็บขยะ-เหรียญ/เข็มทิศ/ไฟฉาย/
> ถ่ายรูป/อัดวิดีโอ/มินิแมพ/เสียง/heatmap/warp/pins/เชือก/บัญชี-สิทธิ์/รายการโปรดออฟไลน์ **ไม่มี WO รองรับเลย**)
>
> ที่มาของรายการ: `id=`, `title=`, และชื่อ `function` ทั้งหมดใน `public/builder.html` (4,408 บรรทัด)
> วิธีอ่านสถานะ: ✅ ทำแล้ว · ⚠️ ทำบางส่วน · ❌ ยังไม่มี
>
> ## 🔴 นับใหม่ทั้งไฟล์ 2026-07-31 — ตัวเลขเดิมเชื่อไม่ได้
> ตัวเลขที่เคยรายงาน (**44.5 / 86**) ผิด **2 จุด**:
> 1. **ตัวหาร 86 ไม่มีที่มา** — นับแถวจริงได้ **83 ข้อ** (A7 B9 C6 D10 E8 F4 G5 H6 I14 J8 K6)
> 2. **หัวข้อหมวดค้าง** ไม่ได้อัปเดตตามแถวที่ปิดไป (A เขียนว่า 5/7 ทั้งที่ทุกแถว ✅ · B 5.5 ทั้งที่ 7.5 · C 4.5 ทั้งที่ 5.5 · D 8 ทั้งที่ 9 · E6 ไม่มีช่องสถานะเลย)
>
> ## **สรุปตัวเลข: 60.9 / 83 ≈ 73%**
> (ตัดข้อที่ user สั่งไม่ทำ D6 อัดวิดีโอ ออก → **60.9 / 82 ≈ 74%** ของงานที่ยังอยู่ในแผน)
> 30 ก.ค. คืน = 43.0 · 31 ก.ค. เช้า = 43.9 · **31 ก.ค. (รอบนี้) = 60.9 · +17.0 ข้อ**
>
> ที่ปิดไปในรอบนี้: **I 0 → 11.5** (palette/วางวัตถุ/gizmo/ก๊อป/ลบ/สี/ชื่อ/รายการวัตถุ/undo-redo/ประวัติ/sculpt)
> · **J 0 → 5** (login/โปรไฟล์/autosave/รายการโปรด) · **A4 0.5 → 0.9** · **E5 0.5 → 1** · **E6 0 → 0.5**
> ถ้าวัดแค่ "ภาพนิ่งของฉาก" ≈ 85%
>
> วิธีให้คะแนน: ✅ = 1 · ⚠️ = 0.5 · ❌/🚫 = 0 · A4 = 0.9 (ครบตามภาพแล้ว แต่ ☁ ออฟไลน์/`by You`/ปุ่ม + · บัญชี ยังใช้จริงไม่ได้)
> เวลาอัปเดตไฟล์นี้: แก้ **ทั้ง 3 ที่** — ช่องสถานะของแถว, หัวข้อหมวด, และตัวเลขสรุป ห้ามปล่อยให้ค้าง

## A. ดูแมพ / กล้อง — 6.9/7
| # | ฟีเจอร์เว็บ | หลักฐาน | Unity |
|---|---|---|---|
| A1 | โหลดแมพจาก API + asset manifest | `load()`, `gload()` | ✅ |
| A2 | orbit หมุน/ซูม/แพน (เมาส์ + สัมผัส) | OrbitControls | ✅ |
| A3 | เฟรมเปิดอัตโนมัติให้พอดีเนื้อหา | `frameContent()` | ✅ |
| A4 | รายการแมพ + ค้นหา + thumbnail | `_objList` / API public · **RN `src/app/map.tsx`** = หน้าจริงในภาพ `docs/refs/web-maplist.png` | ✅ **พอร์ตใหม่ทั้งหน้า 31 ก.ค.** — หน้าเต็ม (เลิกเป็น bottom sheet) · การ์ด 2 คอลัมน์รูป 100px · แบนเนอร์ **Play Game!** (เหรียญทองจริง + ▶ เขียว → popup เลือกโลก → ดำลงน้ำเลย) · ปุ่ม **+** · ปุ่ม **บัญชี** · **♡ ถูกใจ** (ยิง `/react` จริง + จำใน `LikedMaps`) · **⋯ เมนู** (เปิดแผนที่/รายงาน) · `by SIAMDIVE` / `by ชื่อคนสร้าง` / `by Community` · ไอคอนแว่นในช่องค้นหา · ค่าทุกตัวถอดจาก RN stylesheet<br>⚠️ เหลือ 2 อย่างที่ผูกกับฟีเจอร์อื่น: **☁ เก็บออฟไลน์** (แอปยังไม่มีที่เก็บแมพในเครื่อง → ไม่ใส่ไอคอนหลอก) · ~~"by You"~~ **ทำแล้ว** (login เสร็จ) · ปุ่ม + / บัญชี → ปุ่มบัญชีทำงานแล้ว, + ยังเป็น toast |
| A5 | มินิแมพ + เรด้าแสดงสัตว์/วัตถุรอบตัว | `drawMinimap()` :3714, `radarBtn` | ✅ มินิแมพ + ปุ่มเรด้า (ซ้าย 14 / บน 174 ตรงตำแหน่งเว็บ · ปิดแล้วปุ่มจาง 45%) |
| A6 | เข็มทิศ (เหนือ = แดง) | `compass` | ✅ P1.2/UI-parity (ใน HUD ทัวร์) |
| A7 | perf HUD (fps/draw calls) | `showPerfHud()` :4059 | ✅ A7 มุมซ้ายบน เขียว #7CFC9A บนดำ 62% · `FPS 58 min41 · 1100 fish` · **min สำคัญกว่าค่าเฉลี่ย** · เปิด/ปิดในหน้าตั้งค่า |

## B. บรรยากาศ / เรนเดอร์ — 7.5/9
| # | ฟีเจอร์เว็บ | หลักฐาน | Unity |
|---|---|---|---|
| B1 | พื้นทราย superellipse + slab + ขอบ haze | `SAND_R`, `applySeabedTint()` | ✅ WO-04.2 |
| B2 | ผิวน้ำ + คลื่นขยับ | `surf`, wave displace | ⚠️ มีจาน+UV scroll ยังไม่มี vertex wave |
| B3 | ฉากหลังไล่สีแนวตั้ง | `waterBg` :662 | ✅ WO-04.2 |
| B4 | fog ใต้น้ำ | `THREE.Fog(0x123a55,…)` | ✅ WO-04.3 |
| B5 | god rays + caustics | ไม่มีในเว็บ (เราทำเพิ่ม) | ✅ ดีกว่าเว็บ |
| B6 | สลับโหมดกลางวัน ☀️ / โหมดน้ำ 💧 เปิด-ปิด | `setEnv()`, `waterModeBtn`, `bright` | ✅ P2b (ปุ่มในเมนู ☰) |
| B7 | heatmap ความลึก + legend | `setDepthView()` :640, `depthLegend` | ✅ P2a (เบคเป็น texture + legend ตำแหน่งเว็บ) |
| B8 | FX ทองเรืองแสง + หนวดไหว | `_fxGold()`, `_fxBeard()` | ❌ |
| B9 | vignette + murk ตอนอยู่ในทัวร์ | `vignette`, `murkUI()` | ✅ P1.2a |

## C. สัตว์ / AI — 5.5/6
| # | ฟีเจอร์เว็บ | หลักฐาน | Unity |
|---|---|---|---|
| C1 | ฝูงปลา boids + สัตว์ใหญ่ว่ายวน | `buildSchool()`, `schoolStep()` | ✅ WO-03 |
| C2 | ปลาเป็นโมเดล GLB จริง | `InstancedMesh` ใน `buildSchool` | ✅ WO-04.1 |
| C3 | หลบสิ่งกีดขวาง (solid avoidance) | `computeObsR()`, `ejectFromSolids()` | ✅ |
| C4 | genome ต่อสายพันธุ์ + locomotion จาก animation | `speciesGenome()`, `deriveLocomotion()` | ⚠️ ใช้ตารางค่าคงที่ ไม่ derive จาก clip |
| C5 | ปลาตกใจ/หนีผู้เล่น + หาที่หลบ | `schoolFlee()`, `shelterSense()`, `senseAgents()` | ✅ C5 (panic 0..1 · โดรนเร็ว>11 u/s · bait ball · วิ่งเข้าที่กำบัง · genome rank/diet ครบ) |
| C6 | เสียงสัตว์ตามระยะ | `_animalSfxTick()` | ✅ P1.2b |

## D. ทัวร์ดำน้ำ (โดรน) — 9/10 (D6 อัดวิดีโอ = ตัดออกจาก v1 → เต็มที่ทำได้คือ 9)
`enterTour()` :3635 · `exitTour()` · `tourUpdate()`
| # | ฟีเจอร์ | หลักฐาน | Unity |
|---|---|---|---|
| D1 | เข้า/ออกโหมดทัวร์ + ล็อกจอแนวนอน | `enterTour`, `tourLockLandscape()` | ✅ P1.1 |
| D2 | จอยสติ๊ก 2 ตัว (ขึ้น-ลง/เลี้ยว + เดินหน้า) | `makeStick()`, `stickL`, `stickR`, `knobL/R` | ✅ P1.1 (ยังเป็นแป้นเหลี่ยม รอทำวงกลม) |
| D3 | ไฟหน้าโดรน (โคนแสง + spotlight) | `_applyHeadlight()`, `lightBtn`, `mkBeam()` :3669 | ✅ P1.2a (+สลับบรรยากาศ) |
| D4 | อ่านค่าความลึกสดขณะบิน | `tourDepth`, `depthMetres()` | ✅ P1.1 |
| D5 | **ถ่ายรูป** | `tourShot`, `captureThumb()` | ✅ ซ่อน HUD + MediaStore (รอเทสเครื่องจริง) |
| D6 | **อัดวิดีโอ + นาฬิกาจับเวลา** | `tourRec`, `stopRec()` :3801, `_recClock()` | 🚫 ตัดออกจาก v1 (user เคาะ) |
| D7 | โพรงดันปลาออกจากโดรน | `droneBubble()` | ✅ P1.2a |
| D8 | ชนวัตถุแล้วถูกดันออก | `_ejectFromSolids()` | ✅ P1.1 |
| D9 | เข้าทัวร์อัตโนมัติ + จุดเกิดสุ่ม | `_startArenaPlay()`, `enterTour(randomStart)` :3722 | ✅ D9 (จุดเกิดสุ่มในวงแหวน 20-80% ของรัศมี · สูงเหนือพื้น 18u · วาปแล้วลงน้ำเลยไม่เด้งไปหน้าเมนู) · ⚠️ **แก้คำอธิบายเดิมที่ผมจดผิด**: เว็บ**ไม่มี**โดรนบินเอง (`tourCam`=ปุ่มกล้อง, `_tourInstBuild`=รวม draw call) |
| D10 | สอนท่าเล่นครั้งแรก | `_tutTour()`, `_guideRun()` | ✅ D10 (spotlight ทีละปุ่ม 7 ขั้น · ข้าม/ถัดไป/แตะฉากมืด · ครั้งเดียวต่อเครื่อง) |

## E. เกม (เก็บขยะ / เหรียญ / ร้านค้า) — 7.5/8 (E6 ครึ่งเดียว)
| # | ฟีเจอร์ | หลักฐาน | Unity |
|---|---|---|---|
| E1 | ขยะเกิดในแมพ (หลายชนิด + sprite) | `spawnTrash()`, `_buildTrash()`, `_trashSprite()` | ✅ P3a (5 ชนิดตามน้ำหนักเว็บ + ป้าย ♻️ ลอยเหนือ วาดเป็น texture ไม่ใช้ emoji เหมือนเว็บ) |
| E2 | เก็บขยะ (แตะ/ว่ายชน) | `collectTrash()` :4087, `_pickTrash()` | ✅ P3a (รัศมี 11u + คอมโบ + โบนัสความสูง) |
| E3 | เหรียญเกิด + บินเข้ากระเป๋า | `_spawnCoin()` :4069, `flyCoin()` | ✅ P3a (รอบ 60 วิ ×3 + +N ลอยเข้าป้าย) |
| E4 | ยอดเหรียญ + เซฟ/โหลด (ออนไลน์+ออฟไลน์) | `coinUI()`, `saveCoins()`, `_offlineCoinNote()` | ✅ P3b ต่อ `/api/wallet` แล้ว (keyed deviceId เหมือนเว็บ ไม่ต้อง login) · โหลดตอนเริ่มเกม/ส่งตอนออก/ส่งส่วนต่าง earned-spent · เน็ตล่ม = คิวไว้ส่งรอบหน้า · CI ยืนยัน `[Wallet] seeded 600 (Success)` · ข้ามเครื่องต้องรอ login (J) |
| E5 | ร้านค้า — ซื้อสัตว์มาปล่อย | `openShop()` :4301, `buyAnimal()`, `priceOf()`, **`tryPlace()` :4298** | ✅ **แก้ที่พอร์ตผิดหน้าแล้ว 31 ก.ค.** — palette = ร้านค้าจริง (ชิป 10 หมวด · 216 ชิ้น · thumbnail จากเซิร์ฟเวอร์ · 🪙 ราคาบนการ์ด) · **วางวัตถุ = ซื้อ** ตาม `tryPlace` · ปุ่ม 🛒 เปิด palette แทน list · ทางหักเหรียญรวมเป็น **ทางเดียว** ใน `PlaceItem` (list เดิมเรียกตัวนี้) · QC ยืนยัน 450→400 + stock 0→1
| E6 | โหมด arena / เลือกโลกแล้วเล่น | `_startArenaPlay()`, `_arenaExitGate()` | ⚠️ **ครึ่งเดียว** — แบนเนอร์ Play Game! → popup เลือกโลก → ลงน้ำทันที (`_startArenaPlay` ครบ) · ยังไม่มี **ประตูออกจาก arena** (`_arenaExitGate`) |
| E7 | เสียงเอฟเฟกต์ + เพลงบรรยากาศ + ปิดเสียง | `_playSfx()` :4381, `_ambPlay()`, `_toggleMute()` | ✅ P1.2b |
| E8 | ประตูวาปให้ผู้เล่นเดินทางข้ามแมพ | `_warpMenu()`, `_doWarp()`, `_warpFlash()` | ✅ E8 (วงแหวนหมุน+แกน+ผงลอย 18 · เข้าใกล้ 13u เปิดเมนูปลายทาง · CI ยืนยันแล้ว) |

## F. AR / Holomap — 0/4 ❌
| F1 | AR วางแมพในกล้อง (WebXR) | `enterAR()` :2904 · Unity แผน = WO-02m |
| F2 | Holomap ใช้ไจโรหมุนดู | `applyGyro()` :2901, `holostart` |
| F3 | วางโฮโลแกรมข้างหน้าตัว | `placeHoloInFront()` |
| F4 | แถบควบคุม AR (ย่อ/ขยาย/ออก) | `arctl`, `arPlus/arMinus`, `exitAR` |

## G. pins + มีเดีย — 3/5
| G1 | หมุด 📍 ในแมพ | `placePin()` :2876, `makePinSprite()` | ✅ วาดเป็น texture (ไม่ใช้ emoji) · unlit · ลอยเหนือจุด 6u ขนาด 9u ตามเว็บ |
| G2 | แตะหมุดเปิดดู | `openPin()` :2883 | ✅ เลือกด้วย ray (หมุดชนะวัตถุใต้มัน) |
| G3 | เลื่อนดูรูปในหมุด | `renderPin()` :2890, `pinNav` | ✅ แผ่นล่าง + ตัวนับ "3/7" + ก่อนหน้า/ถัดไป · 🔒 กรอง url เหลือแค่ http/https · วิดีโอยังไม่เล่น (บอกตรงๆ ว่าเป็นคลิป) |
| G4 | เพิ่มรูป/วิดีโอเข้าหมุด | `addMedia()`/`saveMedia()` | ❌ ต้องมีสิทธิ์แก้แมพก่อน (J) |
| G5 | ปักหมุดใหม่ | `togglePinMode()` | ❌ อยู่ในโหมดแก้ไข (I) |

## H. เชือก 🪢 — 0/6 ❌
`_startRope()` :3198 · `_startRopeFree()` :3197 · `catenaryPts()` (ท้องเชือกห้อย) · `_editRope()` (sag/หนา/สี) · `_reanchorRope()` · `removeRope()`

## I. Builder แก้ไข/สร้าง — 11.5/14 ✅ (จาก 0/14 เมื่อ 30 ก.ค.)
> **palette = ร้านค้าที่ผู้ใช้เจอจริง** ไม่ใช่ 'เครื่องมือ builder' — บนเว็บการวางวัตถุคือการซื้อ (`tryPlace` หักเหรียญ)
> ทางเข้าสำหรับผู้ใช้: แตะวัตถุ → การ์ดข้อมูล → ปุ่ม ✎ · หรือเมนู ☰ → 📋 รายการวัตถุ / 🕘 ประวัติ / ⛰️ ปั้นพื้น
> ทั้งหมดโชว์เฉพาะเมื่อ `canEdit` จาก server = true

| # | ฟีเจอร์เว็บ | หลักฐาน | Unity |
|---|---|---|---|
| I1 | palette + หมวด + variants | `buildCats`, `showVariants` | ✅ ชิป 10 อัน + กริด 216 ชิ้น + thumbnail PNG จากเซิร์ฟเวอร์ |
| I2 | วางวัตถุ | `placeObj`, `tryPlace`, `moveGhost` | ✅ วาง = ซื้อ · ทางเดียวใน `PlaceItem` · เซฟลงแมพจริงถ้ามีสิทธิ์ |
| I3 | gizmo ย้าย/หมุน/สเกล | `tc` (TransformControls) | ✅ `GizmoMath` + `GizmoController` · เทส 15 · QC ยืนยัน yaw ตรงสูตร + 1 ท่า = 1 history |
| I4 | multi-select + pivot + snap | `_msStart`, `_msSnap` | ❌ |
| I5 | ก๊อป | `dupSelected` | ✅ QC ยืนยัน 14→15 |
| I6 | ลบ | — | ✅ (แถบเครื่องมือ + รายการวัตถุ) |
| I7 | เปลี่ยนสี | `recolorSelected`, `buildColorBar` | ✅ 8 สี ROCK_COLORS + ↺ คืนสีเดิม · โชว์เฉพาะหินตาม `isRecolorable` |
| I8 | ตั้งชื่อวัตถุ | `_objName` | ✅ ผ่าน `TextPrompt` |
| I9 | รายการวัตถุ + ค้นหา + จัดกลุ่ม | `_olRender`, `_olSearch` | ✅ ค้นหา + กรองตามชนิด + เลือก/ตั้งชื่อ/ลบ · QC 14 แถว |
| I10 | undo/redo | `pushHist`, `undo`, `redo` | ✅ snapshot 60 สเตท · **ยกบั๊ก "แมพหาย#2" ของเว็บมาเป็นเทสถาวร** |
| I11 | ประวัติเวอร์ชัน + กู้คืน | `openRevModal`, `restoreRev` | ✅ · ⚠️ สิทธิ์กู้คืนเข้มกว่าแก้ไข (เจ้าของเท่านั้น) UI บอกตรงๆ |
| I12 | ล้างทั้งหมด | `histClearScene` | ⚠️ `MapEditor.ClearAll()` พร้อม ยังไม่มีปุ่ม |
| I13 | **sculpt แปรง 6 แบบ** | `applyBrush` :2798 | ✅ ขุด/ก่อ/ขนาด/ความแรง/สุ่ม/รีเซ็ต · เทส 18 · QC peak 0.00→1.98 · รีบิลด์เฉพาะพื้น |
| I14 | ตั้งพื้นที่/ความลาด/ระดับน้ำ | `applyArea`, `waterRange` | ❌ |

## J. บัญชี / เซฟ / แชร์ — 5/8 (จาก 0/8 เมื่อ 30 ก.ค.)
| # | ฟีเจอร์เว็บ | หลักฐาน | Unity |
|---|---|---|---|
| J1 | บันทึก + autosave | `doSave()`, `autosaveTick()` | ✅ `MapEditor` autosave 1.3 วิ · **ส่ง `baseRev` ทุกครั้ง** (เว็บไม่เคยส่ง ดู IMPROVEMENTS A4) · ไม่ยิงระหว่างลาก · ส่ง `env` ไปด้วย (sculpt) |
| J2 | ตั้งชื่อ + สาธารณะ/ส่วนตัว | `openNameModal`, `setPubLabel` | ⚠️ `MapSaveClient.Rename/SetPublic/SetSearchable` พร้อม ยังไม่มี UI |
| J3 | thumbnail | `captureThumb` | ❌ |
| J4 | login รหัส/อีเมล | `openLogin`, `_lgCodeStep` | ✅ อีเมล → OTP 6 หลัก → ตั้งชื่อ + admin passcode · เทส 15 |
| J5 | โปรไฟล์ | `_profileSheet` | ✅ avatar/ชื่อ/อีเมล/ออกจากระบบ/ลบบัญชี (แตะ 2 ครั้ง) |
| J6 | ให้สิทธิ์แก้ไขทางอีเมล | `openPermission` :3399 | ⚠️ `MapSaveClient.SetEditors` พร้อม ยังไม่มี UI |
| J7 | รายการโปรด + ทัวร์ออฟไลน์ | `_toggleFav` :3082 | ⚠️ รายการโปรดครบ (server-side) · ออฟไลน์ ❌ |
| J8 | กันออกทั้งที่ยังไม่เซฟ | `leaveModal` | ❌ (`MapEditor.Flush()` พร้อมแล้ว ยังไม่มีคนเรียก) |

## K. UI ทั่วไป — 5/6
| K1 | เมนู ☰ + navigation | ✅ WO-05.1 |
| K2 | การ์ดข้อมูลวัตถุ (ชื่อ/ชนิด/ความลึก) | ✅ WO-05.3 — เว็บมีรูป/มีเดียในการ์ดด้วย → ⚠️ |
| K3 | หน้าตั้งค่า | ✅ WO-05.4 |
| K4 | i18n | ⚠️ Unity ไทย/อังกฤษ · เว็บ 5 ภาษา (th/en/cn/ja/de ตาม routes) |
| K5 | toast แจ้งเตือน | ✅ P0 (กลางจอ — **ตรงกับเว็บแล้ว**: `#toast{top:50%;left:50%;translate(-50%,-50%)}` :167 · โน้ตเดิมที่ว่า "เว็บอยู่ล่าง" ผิด) |
| K6 | tutorial / tip cards | ✅ D10 spotlight ครบ + hint line ในทัวร์ · หมายเหตุ: `#hint` ของเว็บเป็นของโหมด **แก้ไข** ไม่ใช่โหมดดู (`body.view #hint{display:none}` :184) → ไม่ต้องทำในหน้าดูแมพ |

---

## สรุปที่ต้องเข้าใจให้ตรงกัน
1. **แอปตอนนี้ = "ดูแมพสวยๆ"** — เรนเดอร์ใกล้เว็บแล้ว (~85% ของภาพนิ่ง) แต่**สิ่งที่ผู้ใช้ทำได้**ยังน้อยมาก
2. **ก้อนใหญ่ที่ไม่มี WO รองรับ** และต้องเพิ่มเข้า roadmap:
   - **WO ใหม่ "Viewer Parity"** = ทัวร์โดรน (D) + เครื่องมือดู (มินิแมพ/เข็มทิศ/ถ่ายรูป/อัดวิดีโอ/heatmap/โหมดแสง) ≈ 2-3 สัปดาห์
   - **WO ใหม่ "Game"** = ขยะ/เหรียญ/ร้านค้า/เสียง/warp (E) ≈ 2 สัปดาห์
   - **WO ใหม่ "Account & Offline"** = login/สิทธิ์/รายการโปรด-ออฟไลน์ (J) ≈ 1-1.5 สัปดาห์
   - pins+มีเดีย (G) และเชือก (H) อยู่ใน WO-09 แล้ว แต่ถูกเลื่อนไปสุดท้าย ทั้งที่เป็นของที่ **ผู้ใช้เห็นในโหมดดู** ไม่ใช่แค่เครื่องมือ builder
3. ลำดับที่แนะนำ (ผู้ใช้ได้ของเร็วสุดก่อน): **ทัวร์โดรน+ถ่ายรูป/อัดวิดีโอ → เข็มทิศ/มินิแมพ → เกมเก็บขยะ+เหรียญ → pins/มีเดีย → login+รายการโปรด → AR → builder แก้ไข**

---

## 📋 งานที่เหลือทั้งหมด — 38.1 ข้อ (ไม่นับ D6 ที่ตัดออก)
เรียงตาม "ผู้ใช้เห็นผลเร็วสุด" ตามคิวที่ user เคาะไว้ใน HANDOFF §4.97

| ลำดับ | ก้อนงาน | ข้อที่ปิดได้ | น้ำหนัก | หมายเหตุ |
|---|---|---|---|---|
| **2** | **palette = ร้านค้าจริง** | E5 (0.5) + I-palette/variants (1) | **1.5** | หนักสุด = เรนเดอร์ thumbnail 3D จาก GLB เอง |
| **3** | **วางวัตถุจริง** (แตะ→วาง=ซื้อ) | I-วางวัตถุ (1) | **1** | ทำให้ข้อ 2 มีความหมาย · `tryPlace` หักเหรียญ |
| **4** | **บัญชี / login** | J ทั้งหมด (8) + A4 เศษ (0.1) | **8.1** | ปลดล็อก `by You` · ปุ่ม + · ปุ่มบัญชี · รายการโปรด+ออฟไลน์ · สิทธิ์แก้ไข |
| **5** | **Builder ที่เหลือ** | I ที่เหลือ (12) | **12** | gizmo · multi-select · ก๊อป/ลบ/สี/ชื่อ · รายการวัตถุ · undo/redo · ประวัติเวอร์ชัน · sculpt 6 แปรง · area/water |
| **6** | **เชือก 🪢** | H (6) | **6** | catenary + แก้ sag/หนา/สี + ย้ายจุดยึด |
| **7** | **AR / Holomap** | F (4) | **4** | ARCore — WO-XR-02m |
| **8** | pins ที่เหลือ | G4 G5 (2) | **2** | ต้องมีสิทธิ์แก้แมพก่อน (ผูกกับ 4) |
| **9** | เก็บตก | B2 (0.5) B8 (1) C4 (0.5) E6 (0.5) K2 (0.5) K4 (0.5) | **3.5** | คลื่นผิวน้ำ · FX ทอง/หนวด · locomotion จาก clip · ประตูออก arena · รูปในการ์ด · i18n 5 ภาษา |

รวม **38.1** → ปิดครบ = 82/82
