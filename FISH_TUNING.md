# การจูนฝูงปลา — คู่มือสำหรับ session ถัดไป

เขียนหลังจากใช้เวลา **สามคืน** ไล่อาการเดียว ("ปลาสั่น/สะบัดหางเร็ว/ไถลข้าง") แล้วแก้ผิดจุดไป
สิบกว่ารอบ. ทุกหัวข้อในนี้คือกับดักที่เหยียบมาแล้วจริง — อ่านก่อนแตะโค้ดฝูงปลาทุกครั้ง.

---

## 1. อาการที่ user รายงาน แปลว่าอะไรได้บ้าง

ผู้ใช้ตัดสินจากเครื่องจริงคนเดียว และบรรยายเป็นภาษาคน. คำเดียวกันมาจากคนละระบบได้:

| คำที่ user ใช้ | เป็นไปได้ว่าคือ | อยู่ที่ไหนในโค้ด |
|---|---|---|
| "สะบัดหางเร็ว" | คลื่นลำตัวใน shader | `SwimStyle.BeatHz` → `_WavePhase` |
| | **เฟสประจำตัวถูกเขย่า** ← ตัวจริงรอบนี้ | `DM_FishWave.cginc / DM_InstancePhase` |
| | โบกทั้งแท่งเมื่อโหลดโมเดลไม่ติด | `FishSchoolSystem` `if (!bends)` |
| "สั่น/กระตุก" | fps ตก / `targetFrameRate` | `AppBoot` |
| | ประตูความกลัวกระพริบ | `FleeMath.DiverIsThreatening` |
| | ทิศหัวแกว่งจาก sim | `BoidsJob` + `DrawRot` |
| "ไถลข้าง" | ตัวเดินไม่ตรงกับจมูก | `BoidsJob.FormationStep` (calm) |
| "ฝูงโหรงเหรง" | รัศมีฝูงคิดจาก WebCount ไม่ใช่จำนวนที่วาดจริง | `MarineMath.ShoalRadius` |

**กฎเหล็ก**: อย่าเริ่มแก้จนกว่าจะรู้ว่าอาการอยู่ระบบไหน — สิบรอบแรกของงานนี้เสียไปกับการจูน
`BeatHz` ทั้งที่อาการอยู่ในแฮชของ shader ซึ่งไม่เกี่ยวกันเลย.

---

## 2. คำถามเดียวที่ตัดปัญหาได้เร็วที่สุด

> **"ปลาชนิดเดียวกัน ในฝูงเดียวกัน ทำเหมือนกันทุกตัวไหม"**

- **ทุกตัวเหมือนกัน** → สาเหตุเป็นค่าที่ทั้งฝูงใช้ร่วมกัน: `BeatHz`, `_WavePhase`, material,
  พารามิเตอร์รูปฝูง
- **แต่ละตัวไม่เหมือนกัน** → สาเหตุต้องเป็นค่าที่แยกต่อตัวเท่านั้น: เฟสประจำตัว, slot, ขนาดสุ่ม,
  สถานะการเลี้ยวของตัวนั้น

คืนที่สาม user พิมพ์มาว่า *"บางตัวหางไม่ขยับ และหลายตัวสะบัดเร็วมาก"* — ประโยคเดียวนี้ตัด
สมมติฐานทิ้งได้หมดทันที และชี้ตรงไปที่เฟสประจำตัว. **ถามคำถามนี้ก่อนวัดอะไรทั้งสิ้น.**

---

## 3. กับดักของเครื่องมือ (เจอครบทุกแบบแล้ว)

เครื่องมือที่ "ไม่มีมิติที่บั๊กอยู่" จะรายงานว่าไม่มีบั๊กเสมอ:

1. **dt คงที่** — SchoolClip ตรึง `Time.captureFramerate` และ sim ทุกตัวใช้ dt คงที่
   → บั๊กที่เกิดจาก dt แกว่งมองไม่เห็นโดยโครงสร้าง
2. **ล็อกรูปฝูงเพื่อให้เทียบง่าย** → ไม่เห็นว่า **ฝูงอยู่ระหว่าง "เปลี่ยนรูป" 84% ของเวลา**
   (hold 15-29 วิ แต่ morph 28 วิ) ⇒ วัดแต่สถานะที่กินเวลา 16%
3. **มุมกล้องขึ้นกับสิ่งที่กำลังวัด** → คลิป "ก่อน/หลัง" ถ่ายคนละมุม เทียบไม่ได้
4. **คลิปบังเอิญจับโหมดที่ไม่มีอาการ** → ก่อนและหลังแก้วัดได้เท่ากัน (4.8°) ทั้งคู่
5. **rig จำลองแต่ชั้น sim ไม่เคยจำลอง shader** → สามคืนวัดผิดชั้น

> **ก่อนเลือกว่าจะวัดสถานะไหน ให้วัด "สัดส่วนเวลาของแต่ละสถานะ" ก่อน**
> **และก่อนสรุปว่า "จำลองแล้วไม่เจอ" ต้องมี positive control** — ป้อนเงื่อนไขที่รู้ว่าอาการเกิด
> แล้วดูว่าเครื่องมือติดไฟไหม ไม่งั้นแยกไม่ออกว่า "ไม่มีบั๊ก" กับ "เครื่องมือตาบอด"

---

## 4. เครื่องมือที่มีอยู่แล้ว (ใช้ต่อ อย่าสร้างใหม่)

| เครื่องมือ | ใช้ทำอะไร |
|---|---|
| `tools/school_sim/school_dt_sim.py` | พอร์ต calm formation + pod มี **แกน dt** · `--report --truth --variants --chart --gif` · อ่านแมพจริงผ่าน `calm_from_map()` · สวิตช์ `nose_follow / nose_knee / nose_cap / axis_breathe / morph_free_nose / gate_fix` |
| `tools/school_sim/measure_video_shake.py` | วัดแอมพลิจูด/ความถี่ของอาการจากวิดีโอ user (auto-threshold) |
| **badge บนจอ** (`FpsBadge`) | `60 fps · b368 · ปลา 10/10` — บิลด์ที่รันจริง + ฝูงที่โหลด GLB สำเร็จ/ทั้งหมด · **5/5 = Harddeep · 10/10 = Chang** |
| SchoolClip ใน CI | `-clipmap <shortId>` `-clipmode cluster\|stream\|vortex` `-clipnose off\|cap\|full` · กล้องเล็งจาก **Anchor0** · เฟรม 9 เท่าความยาวตัว |

**ตัวเลขที่ใช้คุยกับ user ได้ผลดี**: แปลง polarisation เป็น **"ปลาหันต่างกันกี่องศา"**
(ฝูงจริงในธรรมชาติ ~26-33°) และแปลงการไถลเป็น **"เฉียงจากจมูกกี่องศา ถ่วงน้ำหนักด้วยระยะที่
เคลื่อนจริง"** — ตัวเลขดิบอย่าง 0.26 ไม่มีความหมายกับใคร

---

## 5. ค่าที่ user เลือกไว้ (ห้ามแก้กลับโดยไม่ถาม)

| ค่า | ที่อยู่ | ค่าปัจจุบัน | ที่มา |
|---|---|---|---|
| อัตราหาง ฝูง | `SwimStyle.UserSlowMulBarracudaTrevally` | 1.00 | เท่าค่าเว็บ |
| อัตราหาง scad | `SwimStyle.UserSlowMulScad` | 0.50 | |
| ระยะกวาดหาง | `SwimStyle.UserAmpMulBarracudaTrevally` | 2.6 | "โบกน้อยไปมากๆๆ" |
| มุมเบนจมูกสูงสุด | `SchoolFormation.CalmNoseCapRad` | 60° | "ยังไถลข้างมากไป" |
| จุดที่จมูกตามเต็มที่ | `SchoolFormation.CalmNoseKnee` | 0.25 | |
| ฝูง Shoal แน่นขึ้น | `MarineMath.ShoalTightenMul` | 0.6 | "ระยะห่างมากไป" |
| ฝูง Shoal ช้าลง | `MarineMath.ShoalSpeedUserMul` | 0.6 | |
| ความเร็วโดรน | `DroneFlight.Speed` | 24 (เว็บ 30) | "ช้าลงอีกนิด" |

🔴 **ตัวคูณทุกตัวกั้น prefix `school:` / `pod:` เท่านั้น — ห้ามแตะ `msh:*`**
user ใช้ **barracuda ตัวเดียวใน Harddeep เป็นมาตรฐานความถูกต้อง** ถ้าตัวคูณหลุดไปโดนมัน
จะพังของที่ user บอกว่าดีอยู่แล้ว (เคยเกิดแล้ว — เทส `SoloBarracuda_*` จับได้)

---

## 6. บทเรียนเรื่องการรับ feedback

- ทุกครั้งที่ user บอก "เร็วเกินไป" เราลดอัตราหางลง: 1.0 → 0.25 → 0.04
  **แต่สิ่งที่เขาเห็นคือสัญญาณรบกวนคนละตัว** ⇒ ลดจนแข็ง แล้วต้องดันกลับขึ้นมาทั้งหมด
- ⇒ **ถ้าแก้ไปหลายรอบแล้ว user ยังบอกว่า "เหมือนเดิม" ให้หยุดปรับค่า แล้วกลับไปถามว่า
  "อาการหน้าตาเป็นยังไง"** — การปรับค่าซ้ำๆ บนสมมติฐานที่ผิดทำให้ระบบเพี้ยนสะสม
- การเลือกค่าจาก GIF/คลิปใช้ได้ **ก็ต่อเมื่อภาพนั้นสะอาด** — ถ้ายังมีบั๊กปนอยู่ ทุกการเลือก
  เป็นโมฆะและต้องเลือกใหม่หลังแก้บั๊ก

---

## 7. เช็คลิสต์ก่อนบอกว่า "แก้แล้ว"

1. `tools/test.sh` ผ่าน + `tools/check.sh` 0 ปัญหา
   🔴 **แต่ `tools/test.sh` ไม่ใช่เทสทั้งหมด** — มันคือ harness .NET ที่คอมไพล์ได้เฉพาะโค้ดที่
   ไม่พึ่ง UnityEngine · เทสที่ต้องใช้ `PlayerPrefs`/`Vector3`/MonoBehaviour (เช่น
   `SettingsStoreTests`, `ItemPickerTests`) **รันเฉพาะบน Unity CI** และเคยจับบั๊กที่เครื่อง
   มองไม่เห็นมาแล้ว (9 ส.ค.: ลด `DroneFlight.Speed` แล้วพรีเซ็ต "สงบ" เลิกเท่ากับโดรน
   build 261 — เครื่องบอกผ่าน 592/592 แต่ CI ล้ม จน iOS job ถูกข้าม ไม่มีบิลด์ออกมา)
   ⇒ **อย่าเพิ่งบอก user ว่า "เทสผ่าน" จนกว่า job `EditMode tests` บน CI จะเขียว**
2. รู้ว่าอาการอยู่ระบบไหน และมีตัวเลข **ก่อน/หลัง** ของระบบนั้น
3. ถ้าเป็นเรื่อง "ความรู้สึกของการเคลื่อนไหว" → ต้องมีภาพเคลื่อนไหวให้ user เลือกก่อน build
4. เช็คว่าตัวคูณไม่หลุดไปโดนสัตว์ตัวเดี่ยว
5. บอก user ว่าบิลด์เลขอะไรมีอะไร — `IOS_BUILD_NUMBER = github.run_number`
   ตรวจได้ด้วย `git merge-base --is-ancestor <commit> <sha ของบิลด์>` **อย่าเดา**
