# IMPROVEMENTS — สิ่งที่ทำให้ดีกว่าเดิมได้

> สมุดบันทึกระหว่างทำงาน (เริ่ม 2026-07-31) — **ไม่ใช่รายการงานที่ต้องทำ**
> PARITY.md = "เว็บมีอะไรที่แอปยังไม่มี" · ไฟล์นี้ = "อะไรที่ทั้งเว็บและแอปทำได้ดีกว่านี้"
>
> วิธีอ่าน: 🔴 = ควรแก้ก่อนปล่อยจริง · 🟡 = แก้ตอนมีเวลา · 🟢 = ของแถม/ทำให้ดีขึ้นแต่ไม่ด่วน
> ทุกข้อมี **หลักฐาน** (ไฟล์:บรรทัด หรือผลทดสอบ) ห้ามเขียนความรู้สึกลอยๆ

---

## A. บั๊กจริงที่เจอในของที่ใช้งานอยู่ (ไม่ใช่งานพอร์ต)

### 🔴 A1 · ปุ่มหัวใจในแอปมือถือที่ขายอยู่ ไม่เคยบันทึกลง DB
`siamdive-rn/src/lib/dive-map-client.ts:73`
```js
react: (shortId, kind, on) =>
  fetch(u(`/api/dive-sites/${shortId}/react`), { ..., body: JSON.stringify({ kind, on }) })
//                                                                          ↑ ไม่มี deviceId
```
แต่ route บังคับ (`react/route.ts:16`):
```ts
if (!b || !b.deviceId) return NextResponse.json({ error: "deviceId required" }, { status: 400 });
```
และ error ถูกกลืนใน `try{...}catch{}` (`map.tsx:125,137`)
→ **ผู้ใช้เห็นเลขขยับ แต่รีเฟรชแล้วหายทุกครั้ง** · แก้ 1 บรรทัด (เติม `deviceId`)
ฝั่ง Unity ส่งถูกแล้วตั้งแต่แรก (`MapReactClient.cs`)

### 🔴 A2 · ProjectSettings แทบทั้งหมดไม่ได้อยู่ใน git
```
$ git ls-files DiveMap/ProjectSettings
DiveMap/ProjectSettings/ProjectVersion.txt      ← มีไฟล์เดียว
```
แปลว่า **ชื่อแอป / bundle id / ไอคอน / การล็อกแนวจอ / คุณภาพกราฟิก ใช้ค่า default ของ Unity ทุก build**
ตอนนี้ยังไม่พัง เพราะยังไม่ขึ้นสโตร์ แต่พอถึง WO-XR-07 (ขึ้น Play) จะกลายเป็นปัญหาทันที
และทำให้ build ไม่ reproducible — คนละเครื่อง build ได้คนละแอป

### 🔴 A4 · เว็บ autosave ทับงานคนอื่นได้ — เซิร์ฟเวอร์มีตัวกันไว้ให้แล้วแต่ไม่มีใครใช้
เซิร์ฟเวอร์เตรียม optimistic-concurrency ไว้ครบ (`[shortId]/route.ts:134`):
```ts
if (typeof b.baseRev === "number" && b.baseRev !== site.rev) {
  return NextResponse.json({ error:"conflict", conflict:true, site: fresh }, { status: 409 });
}
```
แต่ `builder.html:3363` — body ที่ส่งจริง **ไม่มี `baseRev`**:
```js
const body = JSON.stringify({ deviceId: DEVICE, ...(shortId?{}:{shortId:pendingShortId}),
                              ...serialize(), ...(thumbUrl?{thumbUrl}:{}) });
```
เว็บ**เก็บ** `siteRev` ไว้ (`:3364` `siteRev = r.site.rev`) แต่ไม่เคยส่งกลับ
→ ตัวกันชนไม่เคยทำงานเลยสักครั้ง

**ผลจริง**: แมพ `editPolicy:"all"` = **ใครก็แก้ได้** และ autosave ยิงทุก 1.3 วินาที
สองคนเปิดแมพเดียวกันพร้อมกัน → คนที่ autosave ทีหลัง**ลบงานอีกคนทิ้งเงียบๆ** ไม่มีใครรู้
แก้ที่เว็บ = เติม `baseRev: siteRev` 1 คำ แล้วดัก 409

ฝั่ง Unity: `MapSaveClient` ส่ง `baseRev` ทุกครั้งและโชว์ข้อความ "มีคนแก้แมพนี้ก่อน" แทนที่จะทับ

### 🟡 A5 · `canEdit`/`owned` อยู่นอก `site` ในผลลัพธ์ GET → ถูกทิ้งตอน parse
`[shortId]/route.ts:93` ตอบ `{ site:{…}, canEdit, owned }`
แต่ `MapApiClient.ParseSiteResponse` แกะ `site` ออกมาแล้วโยนสองตัวนั้นทิ้ง
→ แอปไม่รู้ว่าบันทึกได้ไหมจนกว่าจะโดน 403 (หลังหักเหรียญไปแล้ว)
แก้แล้วในรอบนี้ (ยัด `canEdit`/`owned` เข้า site ก่อนสร้าง SceneData)

### 🟡 A3 · react API ไม่ idempotent จริง
`react/route.ts:9-13` เขียนเองว่า *"1-per-device is enforced client-side"*
→ ล้าง PlayerPrefs / ลงแอปใหม่ = กดไลก์แมพเดิมซ้ำได้ไม่จำกัด (rate limit 60 ครั้ง/10 นาที ช่วยได้แค่บางส่วน)
แก้จริงต้องมีตาราง `MapReaction(ownerKey, shortId, kind)` — เป็น migration บน Supabase ที่แชร์กัน

---

## B. หนี้ทางเทคนิคในแอป Unity

### ✅ B1 · ไม่มี local compile check → error 1 บรรทัดกิน CI 1 รอบเต็ม — **แก้แล้ว 31 ก.ค.**
เกิดจริง 1 ครั้ง: CI `30596171135` แดงเพราะ `CS0136` ตัวแปรชื่อซ้ำตัวเดียว = เสียไป ~15 นาที
บนเครื่องนี้ไม่มี Unity เลย จึง type-check ไม่ได้ แต่ **ไม่จำเป็นต้อง type-check** ก็จับได้

ทำแล้ว: `tools/check.sh` → `tools/csharp-check/` (Roslyn parser, ไม่ต้องมี UnityEngine.dll)
```
$ ./tools/check.sh
csharp-check: 114 files · 0 problem(s)        ← 0.96 วินาที
```
พิสูจน์ว่าจับของจริงได้ — ป้อนไฟล์เวอร์ชันที่ทำ CI แดงกลับเข้าไป:
```
$ dotnet …/csharp-check.dll /tmp/regress
/tmp/regress/Scripts/UiShell.cs(882,24): CS0136: a local named 'card' is already in scope…
```
**บรรทัดและคอลัมน์ตรงกับที่ Unity รายงานเป๊ะ** (`UiShell.cs(882,24): error CS0136`)

**รอบ 2 (หลัง CI แดงครั้งที่ 2)** — `MapEditor.cs` เรียก `Toast` ซึ่งอยู่ `DiveMap.Runtime.Ui`
แต่ไฟล์อยู่ `DiveMap.Runtime` · **namespace ลูกไม่ได้อยู่ใน scope ของแม่** ต้องมี using
→ เพิ่มกฎที่ 4: type ที่ประกาศ**ในโปรเจกต์นี้** ถูกเรียกด้วยชื่อเปล่าจากไฟล์ที่ namespace+using เอื้อมไม่ถึง
```
DiveMap/Assets/Scripts/Runtime/MapEditor.cs(203,17): CS0103: 'Toast' is declared in namespace
  'DiveMap.Runtime.Ui', which this file cannot see — add `using DiveMap.Runtime.Ui;`
```
ตรงบรรทัด/คอลัมน์กับที่ Unity รายงานอีกครั้ง · ระวัง false positive: รายงานเฉพาะชื่อที่ประกาศ
**ที่เดียวในโปรเจกต์** (ถ้าซ้ำ = กำกวม ปล่อยให้ compiler ตัดสิน) และไม่ใช่ชื่อที่ qualified อยู่แล้ว

**รอบ 3 (หลัง CI แดงครั้งที่ 3)** — `AppBoot.Manifest.Find(…)` (ชื่อจริงคือ `Get`) และ
`WebCoord.Quat` (`Quat` เป็น type พี่น้อง ไม่ใช่ nested) → เพิ่มกฎที่ 5: **member ที่ไม่มีจริง
บน type ของโปรเจกต์** พร้อมบอกรายชื่อ member ที่มีให้เลือก
```
G.cs(107,73): CHK002: 'AssetManifest' has no member 'Find'
  — did you mean one of: All, BaseUrl, Count, FromJson, Get, Load, ManifestUri, Module…
```
ตามได้ 1 ทอด (`AppBoot.Manifest.Get` ทำงานเพราะรู้ว่า `Manifest` เป็น `AssetManifest`)

⚠️ **รอบแรกของกฎนี้ให้ false positive 2 ตัว — ต้องไล่ดูทีละตัวก่อนเชื่อ**
- `BuildResult.Succeeded` = enum ของ **UnityEditor** ชื่อพ้องกับ struct ในโปรเจกต์
- `Spec.Formation` = ชื่อ **property** ไม่ใช่ type
→ แก้ด้วยกฎเดียวคุมทั้งคู่: **ตรวจเฉพาะ type ระดับบนสุด** (nested type ปกติเรียกว่า `Outer.Nested`
อยู่แล้ว ถ้าเจอ `Nested.Member` เปล่าๆ แปลว่าน่าจะเป็นคนละตัว) + ข้ามชื่อที่เป็น member ที่ไหนสักแห่ง
เครื่องมือที่ cry wolf จะถูกเมินภายในสองวัน — ยอมจับได้น้อยลงดีกว่าจับผิด

จับได้แล้ว: syntax ทุกชนิด · `CS0136`/`CS0128` ตัวแปรชนกัน · `using` ชี้ namespace ที่ไม่มีจริง
· `CS0103` type ที่ namespace เอื้อมไม่ถึง · **`CS0117/CS0426/CS1061` member ที่ไม่มีจริง**
จับไม่ได้: argument ผิดชนิด, member ของ type จาก Unity/แพ็กเกจ, generic ผิด → ยังต้องพึ่ง CI
**กฎ: รัน `./tools/check.sh` ทุกครั้งก่อน push**

📊 CI แดงไป 3 ครั้ง (45 นาที) · ทั้งสามเป็น error ที่ตรวจในเครื่องได้ใน 1 วินาที
ทุกคลาสถูกครอบแล้ว — ถ้าแดงอีก จดว่าเป็นคลาสอะไร แล้วเพิ่มกฎ ไม่ใช่แค่แก้แล้วผ่านไป

### 🟡 B2 · ยังใช้ legacy `UnityEngine.UI.Text` ทั้งแอป ไม่ใช่ TextMeshPro
ผลที่จับได้แล้ว: กฎ `LineHeightRatio = 1.511` + `RowHeight()` ที่ต้องระวังทุกครั้ง
(บั๊ก "ชื่อแมพหายทั้งบรรทัด" มาจากตรงนี้ — HANDOFF §5.2)
TMP ให้ SDF (คมทุกขนาด), auto-size, และไม่ทิ้งบรรทัดเงียบๆ
**ต้นทุน**: ต้อง generate font atlas ของ NotoSansThai (ภาษาไทยตัวอักษรเยอะ) และแก้ทุกไฟล์ UI

### 🟡 B3 · `Screen.width` ถูกใช้แทนความกว้างจริงหลายที่
`UiKit.MakeSheet:308` · `ShopSheet.BuildRows:166` ใช้ `Screen.width / CanvasScale` ตรงๆ
ทั้งที่ทุกหน้าถูกวางใน safe area → บนเครื่องที่มีรอยบาก**แนวนอน** จะกว้างเกินจริง
`MapListScreen.ScreenWidth()` แก้แล้ว (หัก `Screen.safeArea`) — ที่เหลือยังไม่

### 🟡 B4 · ThumbnailCache ไม่มี disk cache และไม่เคย evict
`ThumbnailCache.cs:17` — *"kept for the lifetime of the app"*
- เปิดแอปใหม่ = โหลดรูปใหม่หมด (เว็บได้ HTTP cache ฟรี แต่ UnityWebRequestTexture ไม่ได้)
- palette หมวด `MARINE_LIFE` มี **82 การ์ด** → 82 texture ค้างใน RAM หลังเปิดครั้งเดียว
ควรมี LRU + เขียนลง `Application.temporaryCachePath`

### 🟡 B5 · palette สร้าง GameObject ทุกใบ ไม่มี virtualization
`PaletteSheet.ShowKind` สร้างการ์ดครบทุกชิ้นในหมวด (สูงสุด 82)
แต่ละใบ = พื้นหลัง + ขอบ + ไอคอน + รูป + ชื่อ + ราคา + เหรียญ ≈ **7 GameObject** → ~570 ชิ้น/หมวด
บนมือถือ low-end นี่คือ hitch ตอนสลับแท็บ · ควรรีไซเคิลการ์ดตาม viewport

### 🟢 B6 · `UiKit.Css()` คำนวณ `Screen.dpi` + `CanvasScale` ใหม่ทุกครั้งที่เรียก
เรียกหลายร้อยครั้งต่อการเปิดหนึ่งหน้า · cache ไว้แล้ว invalidate ตอน resolution เปลี่ยนก็พอ

### 🟢 B7 · IconPainter rasterise 96×96 ต่อไอคอนบน CPU
ตอนนี้มี ~32 ไอคอนแล้ว (เพิ่ม 9 ตัวรอบ palette) = 32 × 9,216 px คำนวณ distance-to-polyline
cache ต่อ process แล้ว แต่ครั้งแรกที่เปิด palette ยังกระตุก · ถ้าจะเยอะกว่านี้ควร bake เป็น atlas ตอน build

### 🟢 B8 · `ShopSheet` กับ `PaletteSheet` ซ้ำหน้าที่กัน
ตอนนี้ `ShopSheet` เป็น "ทางสำรอง" ตามเว็บ (`openShop()`) — ยังมีเทสและ QC ของตัวเอง
ถ้าไม่มีใครใช้จริง ควรตัดทิ้ง เหลือทางเดียวจะดูแลง่ายกว่า (การซื้อรวมเป็นทางเดียวใน `PlaceItem` แล้ว)

---

## C. เอกสาร/กระบวนการ

### 🔴 C1 · ตัวเลข PARITY เคยผิดทั้งตัวตั้งและตัวหาร (แก้แล้ว 31 ก.ค.)
- ตัวหาร `86` ไม่มีที่มา — นับแถวจริงได้ **83**
- หัวข้อหมวดไม่เคยอัปเดตตามแถว (A เขียน 5/7 ทั้งที่ทุกแถว ✅)
→ เพิ่มกฎในไฟล์แล้วว่าต้องแก้ **3 ที่**: แถว + หัวข้อหมวด + สรุป
**ควรทำต่อ**: สคริปต์ `tools/parity_count.py` นับจากตารางแล้ว fail CI ถ้าเลขไม่ตรง

### 🔴 C2 · บันทึกส่งต่อ (HANDOFF) มีข้อที่จดผิดแล้วถูกเชื่อต่อ
เจอ 2 ข้อใน §4.97 รอบเดียว:
1. *"API ส่ง ownerName มาแล้วแต่โค้ดไม่ได้ใช้"* — **ผิด** โค้ดเดิมใช้อยู่ (`MapListScreen.cs:410-413` ก่อนแก้)
2. *"งานหนักสุดคือเรนเดอร์ thumbnail 3D จาก GLB"* — **ผิด** เว็บโหลด PNG สำเร็จรูป (`builder.html:2694`)
   → งานที่ประเมินไว้ 4-5 ชม. จบใน ~1 ชม.
**บทเรียน**: ทุกข้อใน HANDOFF ที่อ้างพฤติกรรมเว็บ ต้องมี `ไฟล์:บรรทัด` กำกับ ไม่งั้นรอบหน้าเชื่อผิดต่อ

### 🟡 C3 · QC ถ่ายภาษาอังกฤษผิดหน้า
`qc_ui_en.png` ถ่ายหน้า info card — หน้าที่เพิ่งแก้ (รายการแมพ/palette) ไม่เคยถูกถ่ายเป็น EN เลย
ควรมีตาราง "หน้าไหน × ภาษาไหน" แล้ววน ไม่ใช่ถ่ายหน้าเดียวแทนทั้งแอป

### 🟡 C4 · QC พิสูจน์ได้แค่ "วาดออกมาแล้ว" ไม่ได้พิสูจน์ "กดแล้วไปถูกที่"
ตอนนี้เป็นภาพ + log อย่างเดียว · ไม่มีเทสระดับ "กดปุ่ม X แล้วต้องเปิดหน้า Y"
ทำได้ด้วย PlayMode test ที่เรียก `button.onClick.Invoke()` แล้ว assert ว่า sheet ที่ถูกต้องเปิด
(เทสซื้อของ E5 ทำแบบนี้อยู่แล้ว 1 เส้นทาง — ขยายให้ครอบทุกปุ่มหลักได้)

### 🟡 C5 · QC รันแนวนอนอย่างเดียว (1280×720)
แต่หน้าจริงที่ผู้ใช้เจอ (รายการแมพ/palette) ออกแบบมาสำหรับ**แนวตั้ง**
กริด 2 คอลัมน์บนจอ 1280 กว้าง = การ์ดกว้าง 927 หน่วย ซึ่งไม่ใช่ภาพที่ผู้ใช้เห็น
เพิ่ม `-screen-width 720 -screen-height 1280` อีกรอบก็ได้ภาพแนวตั้งมาเทียบกับ `docs/refs/` ตรงๆ

---

## D. ของที่ทำได้ดีกว่าเว็บ (ข้อเสนอ ไม่ใช่ parity)

### 🟢 D1 · โมเดล 3 ระดับ (LOD) — ต้นฉบับความละเอียดสูงมีครบแล้ว
`/root/asset-masters/` **7.6 GB · 207 ไฟล์** ยังไม่ถูกใช้
เว็บย่อทุกอย่างเพราะ WebView พัง GPU (VRAM 90MB→3MB/โมเดล) — **แอป native ไม่ติดข้อจำกัดนี้**
ตัวอย่าง: รูปปั้นสิงห์ raw 47.6 MB / 80k tris / 4K×4 → ที่ใช้จริง 0.46 MB (tris 25%, tex 1024)
⚠️ สัตว์ raw ไม่มี rig → ต้องรัน `optimize → auto-rig → swim clip` ใหม่ทีละตัว

### 🟢 D2 · เตือนก่อนผูกบัญชี (ทำไปแล้วในรอบนี้)
`verify/route.ts:50` ทำ `updateMany({ where:{deviceId, accountId:null}, data:{accountId} })`
= ยืนยัน OTP แล้ว **แมพและเหรียญในเครื่องถูกผูกเข้าบัญชีถาวร ย้อนกลับไม่ได้**
เว็บไม่บอกผู้ใช้เลย · แอปนี้เพิ่มบรรทัดเตือนใน `LoginSheet` แล้ว
→ ควรเสนอให้เว็บ/RN เพิ่มด้วย

### 🟢 D3 · ลบบัญชีต้องแตะ 2 ครั้ง (ทำไปแล้ว)
RN ใช้ `Alert.alert` ซึ่งบนบางเครื่องกดพลาดง่าย · แอปนี้ให้ปุ่มเปลี่ยนข้อความเป็น
"แตะอีกครั้งเพื่อลบบัญชีถาวร" ก่อนจะยิงจริง

---

## E. ค่าใช้จ่าย / โครงสร้างพื้นฐาน

### 🟡 E1 · CI รอบละ ~15 นาที และ repo จะกลับเป็น private 1 ส.ค. 08:05
หลังจากนั้นเหลือโควตาฟรี 2,000 นาที/เดือน · ที่ลดแล้วทั้ง 2 repo ≈ 1,400 นาที/เดือน
→ **เหลือที่ว่างประมาณ 40 รอบ/เดือน** · ทุกรอบที่เสียไปกับ compile error คือ 2.5% ของโควตา
(ดู B1 — local compile check คุ้มมาก)

### 🟢 E2 · thumbnail ของ palette ยังไม่ถูก preload
เปิดหมวดใหม่ = ยิง 82 request พร้อมกัน (cap ที่ 4 ตัวใน `ThumbnailCache.MaxConcurrent`)
ผู้ใช้เห็นไอคอน fallback ไล่หายทีละใบ · ควร prefetch หมวดถัดไปตอน idle

---

---

## F. เจอจากภาพ QC (ไม่ใช่จากการอ่านโค้ด)

### ✅ F1 · เหรียญขึ้นซ้อนกัน 2 อัน ตอนเปิด palette — **แก้แล้ว**
`qc_ui_palette_buy.png` (CI `30596568212`): ป้าย 🪙450 ของ `PaletteSheet` ทับป้าย 🪙450
ของ `CoinCounter` ใน HUD ทัวร์ · แถมเข็มทิศ/ตัวเลขความลึก/ปุ่มกล้อง โผล่รอบๆ sheet
สาเหตุ: บนเว็บ palette อยู่ใน **โหมดแก้ไข** ซึ่งไม่มี HUD ทัวร์เลย แต่แอปเปิดจากในทัวร์
แก้: `TourHud.SetChromeVisible(false)` ตอนเปิด · คืนตอนปิด **และตอน OnDestroy**
(แมพ reload จะทำลาย layer โดยไม่ผ่าน `Close()` → ถ้าไม่คืนใน OnDestroy ผู้เล่นจะเจอทัวร์ที่ไม่มีจอย)

**บทเรียน**: ตรวจ log อย่างเดียวไม่พอ — log บอกว่า `chips=10 cards=15` ถูกหมด
แต่ภาพบอกว่ามีป้ายเหรียญ 2 อัน · **ทุกหน้าใหม่ต้องดูภาพจริง ไม่ใช่แค่ตัวเลข**

### 🟡 F2 · ไอคอนชิปบางตัวอ่านไม่ออกที่ขนาด 23 px
"ปะการังเทียม" (🗿 moai) ดูเหมือนแบตเตอรี่ · "เรือ" (⛵) เล็กเกินจะเห็นใบ
เว็บใช้ emoji จริงซึ่งมีรายละเอียดสีครบ · แอปวาดเป็นเส้นขาวล้วนเพราะฟอนต์ไม่มี emoji
ทางเลือก: วาดแบบมีสี (fill หลายชั้น) หรือใช้รูป PNG เล็กๆ เหมือน thumbnail ของ item

### 🟡 F4 · แถบเครื่องมือเลือกวัตถุทับมินิแมพ (รูปแบบเดียวกับ F1)
`qc_ui_seltool.png` — `#seltool` อยู่ล่างกลาง (bottom 22) ทับมินิแมพของ HUD ทัวร์พอดี
**สาเหตุเชิงออกแบบเดียวกับ F1**: บนเว็บ `#seltool` อยู่ใน**โหมดแก้ไข** ซึ่งไม่มี HUD ทัวร์
→ ทางแก้ที่ถูกกว่าการซ่อน HUD: **การเลือก/แก้วัตถุควรใช้ได้เฉพาะโหมดดูแมพ (orbit) ไม่ใช่ระหว่างทัวร์**
เหมือนเว็บที่ edit ≠ tour · ตอนนี้ QC เรียกตอนอยู่ในทัวร์เพื่อทดสอบเท่านั้น

**รูปแบบที่เห็นซ้ำ 2 ครั้งแล้ว**: ทุกครั้งที่พอร์ตหน้าจาก "โหมดแก้ไข" ของเว็บมาเปิดทับโหมดทัวร์
จะชนกับ HUD เสมอ → ก่อนพอร์ตหน้าใหม่ ให้ถามก่อนว่า *บนเว็บหน้านี้อยู่โหมดไหน* แล้ววางใน
โหมดเดียวกันในแอป ไม่ใช่เปิดทับโหมดที่บังเอิญเปิดอยู่

### ✅ F5 · press ที่ถูกปฏิเสธ ไม่ล้างสถานะ → ท่าลากถัดไปใช้จุดเริ่มของท่าเก่า — **แก้แล้ว**
เจอจาก log ของ CI (ไม่ใช่จากภาพ): ลาก 2 ท่า แต่ history เพิ่มแค่ 1
```
[QC] gizmo move (-4.9,128.0)→(184.9,-285.6) moved=True · yaw=-1.569
     · history 2→3 (expected +2, one per gesture)
```
ต้นเหตุ: `GizmoController.Press()` มี `if (UiShell.PointerOverUi()) return;` **ก่อน**ที่จะเซ็ต
`_pressPos` / `_startYaw` / `_dragging` → press ที่ถูกปฏิเสธทิ้งค่าของท่าที่แล้วไว้
แล้ว `Move()` ท่าถัดไปวัดระยะจาก**จุดเริ่มเก่า** และหมุนจาก**มุมเก่า**
แก้: press ที่ถูกปฏิเสธต้อง **disarm** ท่านั้น (`_armed = false`) ไม่ใช่แค่ return

**2 บทเรียนจากรอบนี้**
1. `RecordAndApply` เดิมเป็น `void` — ไม่มีทางรู้ว่า snapshot ถูก**รับ**หรือถูกปฏิเสธ
   ตอนนี้คืน `bool` เพราะ "ดูเหมือนเซฟแล้ว" คือ failure mode ของฟีเจอร์นี้
2. **ค่าที่ QC เขียนว่า "expected" ผิดเอง** — บรรทัด log เขียน `expected 3.142` โดยสมมติว่า
   yaw เริ่มต้น = 0 ทั้งที่วัตถุหมุนอยู่แล้ว · ตอนนี้อ่าน yaw ก่อนลากมาคำนวณ expected จริง
   → **ค่า expected ใน QC ต้องมาจากสถานะจริงก่อนหน้า ไม่ใช่ค่าที่คิดว่าน่าจะเป็น**

### ✅ F7 · เทส QC วางผิดโหมด แล้วเกือบทำให้เข้าใจว่าโค้ดพัง
รอบถัดมา log ชัดขึ้นเพราะเพิ่งใส่ id ลงไป:
```
[Edit] Rotate released with no drag (tap) for        ← id ว่าง
```
`_id` เป็น null เพราะบล็อกเทสอยู่**ในทัวร์** และ `GizmoController.Update()` จงใจ
`Deselect()` ทันทีที่โหมดไม่ใช่ `View` — ซึ่งคือตัวแก้ F4 ที่เพิ่งใส่เข้าไปเอง
ท่าแรกผ่านเพราะ `QcDrag` ทำ Press→Move→Release ในเฟรมเดียว ท่าที่สองมี `yield` คั่น
→ Update ได้รันระหว่างกลาง แล้วล้าง selection

**โค้ดถูก เทสผิดที่** → ย้ายบล็อกไปไว้ก่อนเข้าทัวร์
**บทเรียน**: เวลา QC บอกว่าฟีเจอร์ไม่ทำงาน ให้ถามก่อนว่า *เทสรันอยู่ในสภาพแวดล้อมที่ฟีเจอร์
ออกแบบมาให้ทำงานหรือเปล่า* — ไม่งั้นจะ "แก้" โค้ดที่ถูกอยู่แล้วจนพัง
(และ log ที่ไม่มี id ทำให้เสียเวลาเดา 1 รอบ — **log ต้องพิมพ์ตัวแปรที่ตัดสินใจด้วยเสมอ**)

### ✅ F8 · แปรงปั้นพื้นทำงานไม่ได้บนแมพที่ไม่เคยถูกปั้น — **แก้แล้ว**
```
[Sculpt] registered rings=28 seg=96 samples=0 sandR=340
[QC] sculpt open=True ready=False samples=-1 peak 0.00→0.00
```
`ReadSculpt(env)` คืน `null` เมื่อแมพไม่มี `env.sculpt` (ซึ่งคือ**แมพส่วนใหญ่**) → แปรงวนลูป
บน array ว่าง = ทุกปาดเป็น no-op เงียบๆ ไม่มี error ไม่มีอะไรเลย
แก้: จอง grid `rings*seg` ค่าศูนย์เสมอ (พื้นเรียบเหมือนเดิม เมชไม่เปลี่ยน) ตั้งแต่ตอน build

**บทเรียน**: ฟีเจอร์ที่ต้อง "แก้ไขข้อมูลที่อาจยังไม่มี" ต้องสร้างที่ว่างให้ก่อนเสมอ
ไม่งั้นจะใช้ได้เฉพาะกับข้อมูลที่มีอยู่แล้ว — และเคสที่ใช้ไม่ได้คือเคสที่พบบ่อยที่สุด
(`ready=False` ใน log คือสิ่งเดียวที่บอกได้ — **ค่า readiness ต้องอยู่ใน log เสมอ** ไม่ใช่แค่ผลลัพธ์)

### 🟡 F6 · ลากนิดเดียวตอนซูมออก = วัตถุกระเด็นไกลมาก
จาก log เดียวกัน: ลาก 120×70 px → วัตถุขยับ (190, −413) หน่วยโลก
เพราะระนาบลากอยู่ที่ความสูงวัตถุ และกล้อง orbit อยู่ไกล → 1 px = หลายหน่วย
เว็บมีคุณสมบัติเดียวกัน (drag บนระนาบ) แต่บนมือถือจอเล็กจะรู้สึกคุมไม่ได้
ทางเลือก: จำกัดระยะขยับต่อเฟรม หรือใช้ความเร็วแบบ non-linear เมื่อกล้องอยู่ไกล

### 🟢 F3 · ชื่อ item ในกริดปนไทย-อังกฤษ
`Acrobat Shrimp` / `Longhorn Shrimp` แต่ `ปู` / `ม้าน้ำ` — มาจาก `name` ใน manifest
เว็บก็เป็นแบบเดียวกัน (ไม่ใช่ regression) แต่ควรทำให้เป็นภาษาเดียวทั้งกริด

---

## รอบ AR (31 ก.ค.)

### 🔴 G1 · `ProjectSettings/` ไม่อยู่ใน git — ตอนนี้ขวางงาน AR จริงๆ แล้ว
เดิมจดเป็นความเสี่ยง "build ไม่ reproducible" ตอนนี้มันกลายเป็นข้อจำกัดที่ทำให้**เลือกไม่ใช้ ARCore**:
ARFoundation ต้องตั้งค่า XR plugin ใน `ProjectSettings/` ถ้าใส่ไป มันจะทำงานเฉพาะเครื่องที่ตั้งค่า
แล้วเงียบๆ ตกกลับเป็นไม่มีอะไรบน CI/เครื่องอื่น → เลือกทำแบบเว็บ (ฟีดกล้อง+เซนเซอร์) แทน
**ผลกระทบจริง**: ไม่มี plane detection, ไม่มีการขยับตัวเดินรอบแบบจำลอง
แก้ที่ต้นเหตุก่อนถึงจะอัปเกรดได้ — ไม่ใช่แค่เรื่องความเรียบร้อยอีกต่อไป

### 🟡 G2 · ยังไม่มี `Assets/Plugins/Android/AndroidManifest.xml`
Unity เติม `android.permission.CAMERA` ให้เองเมื่อโปรเจกต์ใช้ `WebCamTexture` — **น่าจะ**
แต่ยังไม่มีใครยืนยันกับ APK จริง ถ้ารอบแรกกล้องไม่ขึ้น นี่คือที่แรกที่ต้องดู
ไม่ใส่ manifest ไปก่อนโดยตั้งใจ: manifest ที่เขียนผิดทำ build พังทั้งก้อน และตรวจ headless ไม่ได้
→ รอผล APK รอบแรกแล้วค่อยตัดสิน

### 🟢 G3 · ฟีดกล้องความละเอียดตายตัว 1280×720
ยังไม่รู้ว่าเครื่องจริงเฟรมเรตพอไหมเมื่อฟีด + ฉากเต็มวิ่งพร้อมกัน
ถ้าตก ให้ลดตามความละเอียดจอ แทนที่จะฝังเลขไว้

### 🟡 G5 · ผิวน้ำมองจากใต้น้ำเป็นแฉกดำทึบ (ไม่ใช่เรื่อง AR — มีมาก่อน)
เห็นชัดใน `qc_ui_menu.png`: จานน้ำจากด้านล่างเรนเดอร์เป็นกลีบดำสลับใส
อยู่ในภาพ QC ทุกใบมานานแล้วแต่ไม่มีใครทัก · เกี่ยวกับ B2 (ผิวน้ำ) ที่ยังค้างอยู่พอดี

### 🟢 G4 · `ArSession` หา seabed/water ด้วย `GameObject.Find("Map")` + ชื่อลูก
ผูกกับชื่อสตริงที่ `SceneBuilder` ตั้ง ถ้าเปลี่ยนชื่อ AR จะไม่ซ่อนพื้นเงียบๆ โดยไม่มี error
ที่ควรเป็น: `BuildResult` คืน reference ของ seabed/water ตรงๆ (เหมือนที่คืน `Obstacles`/`Animals`)

---

## บทเรียนที่ใช้ซ้ำได้ (จดไว้เพราะเสียเวลาไปกับมันจริง)

### ★ "พิสูจน์ไม่ได้" หลายครั้งแปลว่า "ยังไม่ได้เขียนโจทย์ให้ถูก"
เครื่องหมายของเทอมหมุนจอใน AR ถูกจดไว้ว่า "ต้องเทสบนเครื่องเท่านั้น"
พอเขียนใหม่เป็น *"หมุนเครื่อง θ + จอรายงาน θ ต้องหักล้างเป็นศูนย์"* มันเช็คได้ทันทีแบบ headless
และมีไซน์เดียวที่ผ่าน → ก่อนจะยอมรับว่าอะไร "เทสไม่ได้" ให้ลองหาสมบัติที่ต้องเป็นจริงเสมอก่อน

### ★ ตารางที่พังตอน static init จะรายงานความผิด**ผิดที่**เสมอ
คีย์ `"ขนาด"` ซ้ำใน `UiStrings` → เทส **7 คลาส** ล้ม (Account/Palette/MapDirectory/SettingsStore/…)
ทั้งที่ความผิดอยู่บรรทัดเดียว เวลาเห็นเทสล้มเป็นพวงที่ไม่เกี่ยวกัน ให้สงสัย initializer ก่อน
→ เพิ่มการตรวจซ้ำ/latin gate ในเครื่องก่อน push แล้ว (สคริปต์เดียว 1 วินาที เทียบกับ CI 15 นาที)

### ★ log ที่ตัวเองเขียน พิสูจน์ได้แค่สิ่งที่ตัวเองนึกจะเช็ค
รอบแรกของ AR: เทสผ่าน 519/519 · log ทุกบรรทัดเขียว · **แต่เปิดภาพแล้วเจอ 4 ข้อ**
1. พื้นทะเลยังเรืองแสง ทั้งที่ log เขียน `seabedHidden=True` — เพราะ Caustics เป็น**พี่น้อง**ของ Seabed
2. `pos=False` (ข้อเดียวที่ log จับได้เอง) — OrbitCamera คำนวณตำแหน่งใหม่ทุกเฟรมจาก target/distance
3. ปุ่ม ＋ เป็นกล่องว่าง — ฟอนต์ไม่มี glyph U+FF0B
4. **ปุ่ม AR หลุดนอกจอ** — 12 ปุ่ม × 58px + ปุ่ม ☰ = 774 > 720 จอสูงไม่พอ

ข้อ 1/3/4 ไม่มีทางเห็นจาก log เลย เพราะ log พิสูจน์ได้แค่ "GameObject นี้ inactive"
ไม่ใช่ "ผู้ใช้เห็นอะไร" → **ทุกฟีเจอร์ที่มีหน้าตา ต้องมีภาพ ไม่ใช่แค่บรรทัด log**
และ log ควรบอก n/ทั้งหมด (`underwaterHidden=4/4`) ไม่ใช่ bool ตัวเดียวที่กวาดของที่ลืมไว้ใต้พรม

### ★ หา object ด้วยชื่อ = เดา — ถ้าตัวสร้างส่ง reference มาให้ ต้องใช้ reference นั้น
`GameObject.Find("Map")` ทำให้ AR ไปซ่อน**สำเนาที่มองไม่เห็น** หลังจาก QC ซื้อของแล้วแมพโหลดใหม่
log เขียน `underwaterHidden=3/3` ถูกต้องตามที่มันวัด — แต่มันวัดคนละ root กับที่เรนเดอร์อยู่
`Configure(BuildResult)` มี `r.Root` อยู่ในมือแล้วตั้งแต่แรก
**และ QC ต้องอ่าน root ตัวเดียวกับที่โค้ดจริงใช้** ไม่งั้นสองฝั่งเห็นคนละโลกแล้วเห็นตรงกันเองโดยบังเอิญ

### ★ เพิ่มปุ่มใหม่ = ต้องดูว่ามันโผล่จริงไหม
ปุ่มที่ 12 ถูกสร้างสำเร็จทุกอย่าง แค่ตำแหน่งอยู่เหนือขอบจอ — โค้ดถูก ไม่มี error
ผู้ใช้กดไม่ได้เลย · UI ที่วางด้วยพิกัดคงที่ต้องคิดเพดานเสมอว่ากี่ชิ้นถึงล้น

### ★ ตัวเลขความคืบหน้าที่พิมพ์ด้วยมือ ผิดได้ทั้งสองทาง
PARITY หัวไฟล์เขียน 68.9 แต่แถวจริงรวมได้ 73.5 (G4/G5/J3/J8 ทำเสร็จแล้วแต่แถวยังเป็น ❌)
รอบก่อนหน้าก็เคยเขียนสูงเกิน → **นับด้วยสคริปต์จากช่องสถานะเสมอ** อย่าพิมพ์เลขรวมเอง

---

## รอบถัดไปจะจดเพิ่มที่นี่
_(อัปเดตทุกครั้งที่เจอของใหม่ — อย่ารอจนจบงานแล้วค่อยนึกย้อน)_
