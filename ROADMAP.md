# ROADMAP v2 — DiveMap (2026-07-30)

> เขียนใหม่ทั้งหมดเพราะ roadmap เดิมใน `DESIGN_DOC.md §5` วางลำดับตาม "ความว้าวของ XR"
> แต่ไม่มีช่องให้ฟีเจอร์ที่เว็บมีจริงและผู้ใช้ใช้อยู่ทุกวัน — ทัวร์โดรน, เกมเก็บขยะ/เหรียญ,
> เข็มทิศ, มินิแมพ, ถ่ายรูป, อัดวิดีโอ, เสียง, heatmap, warp, pins, เชือก, บัญชี/สิทธิ์, รายการโปรดออฟไลน์
> ฐานความจริงของ "เหลืออะไร" = **`PARITY.md` (86 ฟีเจอร์ · ตอนนี้ ~21%)** ห้ามประเมินจาก WO เพียวๆ อีก
>
> หลักการเรียงลำดับใหม่: **ผู้ใช้ได้ของที่ใช้ได้จริงก่อน → ปล่อยขึ้น Play เป็นแอปผู้ชม → แล้วจึงไปงาน builder**
> (roadmap เดิมเอา builder/headset ขึ้นก่อน ทำให้แอปที่ปล่อยได้จริงถูกเลื่อนออกไปเป็นเดือน)

## หน่วยวัดงาน
1 **รอบ** = แก้โค้ด → push → CI ~35 นาที (Android APK + Windows + QC screenshot) → อ่าน log/ภาพ → ตัดสิน
งานที่ต้องเทสบนเครื่องจริง (มือถือ) ต้องรอ user 1 รอบเสมอ — จัดคิวให้มีของให้เทสเป็นชุด ไม่ใช่ทีละชิ้น

---

## ✅ ข้อตัดสินใจของ user (2026-07-30) — แผนนี้ยึดตามนี้
1. **ลำดับ: P0 → P0.5 → P1 (ทัวร์) ตามแผน**
2. **อัดวิดีโอในแอป: ไม่ทำใน v1** — ให้ผู้ใช้ใช้ปุ่มอัดหน้าจอของมือถือ (ยังทำ "ถ่ายรูป" ปกติ) → ความเสี่ยง R1 ปิด ไม่ต้องแตะ native MediaProjection
3. **ปล่อย Google Play: รอ builder ครบก่อน (หลัง P9)** — ไม่ปล่อยรุ่นผู้ชมก่อน ⇒ P6 ย้ายไปท้ายสุด และ **การเทสทั้งหมดจนถึงตอนนั้นคือ APK sideload ให้ user** (ไม่มี closed test บน Play)
4. **ภาษา: ไทย/อังกฤษ พอสำหรับ v1** — ไม่เพิ่ม cn/ja/de (จะต้องฝังฟอนต์ CJK ทำให้ APK ใหญ่) ไว้ค่อยพิจารณาก่อนปล่อยจริง

## P0 · เก็บค้าง + สิ่งที่ user เคาะ  · 1-2 รอบ
- ลำแสงนุ่ม (billboard + falloff ใหม่) — ✅ อยู่ใน CI แล้ว `804832f`
- ความสว่างทราย/caustics — รอ user ตัดสิน (ค่าเดียว 2 บรรทัด)
- **hull เรือมีแผ่นดำ** = backface ของ wreck GLB → ทำ submesh กลับด้านเพิ่มตอนโหลด (ห้ามแตะ shader keyword)
- ป้ายสถานะหัวจอยังไม่แปลภาษา · โหมดประหยัดยังไม่ลดจำนวนปลา
- **`showToast()`** — จำเป็นก่อน P1 (ทุกโหมดใช้แจ้งสถานะ)
**DoD:** QC ภาพผ่าน + ไม่มีข้อค้างเล็กในลิสต์ K5/B ของ PARITY

## P0.5 · โครงโหมด (ทำก่อน P1 ไม่งั้นแก้ยากทีหลัง) · 1 รอบ
`AppBoot` 443 บรรทัดกำลังกลายเป็นถังรวมทุกอย่าง แยกเป็น
`ModeManager` (View / Tour / Game / AR / Edit) + `HudLayer` (ชั้น UI ต่อโหมด) + `InputRig` (จอย/สัมผัส/gyro reuse ข้ามโหมด — จะได้ใช้ซ้ำตอนทำ AR และ Galaxy XR)
**DoD:** สลับ View↔Tour ได้โดยไม่มี if ซ้อนใน AppBoot · เทส EditMode ของ state machine ผ่าน

## P1 · ทัวร์ดำน้ำ (โดรน) — ก้อนที่ผู้ใช้รอที่สุด · 5-7 รอบ
อ้างอิงเว็บ: `enterTour()` :3635 · `tourUpdate()` · `mkBeam()` :3669 · `drawMinimap()` :3714
- **P1.1 แกนการบิน** — เข้า/ออกโหมด · จอยสติ๊ก 2 ตัว (uGUI legacy ทำมือ ห้าม TMP) · ความเร็ว/หน่วง/ลอยตัวตามสูตรเว็บ · ชนวัตถุถูกดันออก (`_ejectFromSolids`) · ล็อกจอแนวนอน · อ่านความลึกสด (U_PER_M=6) · ปุ่มออก
- **P1.2 บรรยากาศในทัวร์** — ไฟหน้าโดรน (โคนแสง + spotlight) · บับเบิล · vignette/murk · **เสียง**: ambience loop + cue + SFX (ไฟล์มีอยู่แล้วที่ `maps.siamdive.com/audio/*.mp3` 8 ไฟล์ — สตรีมจาก CDN ไม่ต้องยัดลง APK) + ปุ่มปิดเสียง
- **P1.3 บันทึกภาพ/วิดีโอ + ทัวร์อัตโนมัติ** — ถ่ายรูปลงแกลเลอรี (MediaStore ผ่าน AndroidJavaObject + permission) · **อัดวิดีโอ = ต้อง spike ก่อน** (ดูความเสี่ยง R1) · ทัวร์บินเองอัตโนมัติ (`_tourInstBuild`) · tutorial 30 วิครั้งแรก
**DoD:** user บินสำรวจ HTMS Chang บนมือถือได้ลื่น · ถ่ายรูปได้ไฟล์จริงในแกลเลอรี · เห็นไฟหน้า/ได้ยินเสียง

## P2 · HUD เครื่องมือดู · 2-3 รอบ
เข็มทิศ (เหนือ=แดง) · มินิแมพ/เรด้า (วาด `Texture2D` เองแล้วโชว์ใน RawImage) · **heatmap ความลึก + legend** (เบคเป็น texture แบบเดียวกับทราย WO-04.2 เพราะ Standard shader ไม่อ่าน vertex color) · โหมดกลางวัน ☀️ / ปิดน้ำ 💧 · perf HUD (fps/tris)
**DoD:** เทียบภาพกับเว็บทีละปุ่ม ตรงทั้ง 5

## P3 · เกม (ขยะ / เหรียญ / ร้านค้า / warp) · 3-4 รอบ
- ขยะเกิด+เก็บ (หลายชนิด + sprite) · เหรียญเกิด + บินเข้ากระเป๋า + **sync `/api/wallet`** + โหมดออฟไลน์
- ร้านค้าซื้อสัตว์มาปล่อย (`/api/assets` + `priceOf`) · ประตูวาปข้ามแมพ (`_doWarp`) · SFX/เพลง (ต่อจาก P1.2)
- ปลาตกใจหนีผู้เล่น + หาที่หลบ (`schoolFlee`/`shelterSense`) — ทำที่นี่เพราะต้องมีผู้เล่นก่อน
**ผูกกับ P4:** เก็บเหรียญจริงต้อง login → ถ้ายังไม่มีบัญชี ให้เล่นได้แบบออฟไลน์ก่อน (เว็บทำแบบนี้: `_offlineCoinNote`)
**DoD:** เก็บขยะ/เหรียญแล้วยอดตรงกับเว็บ (บัญชีเดียวกัน)

## P4 · บัญชี + รายการโปรด + ออฟไลน์ · 2-3 รอบ
OTP อีเมล (`/api/account/email/request-otp` → `verify`) · โปรไฟล์/ตั้งชื่อ · logout · **รายการโปรด** (`/api/dive-sites/favorites`) · **แคชแมพ+asset ไว้ทัวร์ออฟไลน์** (ของที่เว็บทำได้แล้ว)
**DoD:** ปิดเน็ตแล้วยังเปิดแมพที่ใส่รายการโปรดไว้ + ทัวร์ได้

## P5 · pins + มีเดีย + การ์ดข้อมูลเต็ม · 2 รอบ
pin ในฉาก + แตะดู · รูป/มีเดียในการ์ด (`/api/dive-sites/media`) · เลื่อนดูหลายรูป
**DoD:** การ์ดข้อมูลมีรูปเหมือนเว็บ (ตอนนี้มีแต่ตัวหนังสือ)

## P6 · ปล่อยขึ้น Google Play — 🔚 **ย้ายไปท้ายสุด ตามที่ user เคาะ** · 2-3 รอบ + งานใน console
onboarding · error/offline UX · icon + listing + privacy · perf gate บนเครื่องจริง · AAB + signing · (i18n เพิ่มภาษาถ้าตัดสินใจเพิ่มตอนนั้น)
**DoD:** แอปอยู่บน Play — ทำ **หลัง P9** พร้อมกับ P10
**ระหว่างนี้ทุกการเทส = APK sideload ผ่าน `dive3d.suksomsri.cloud/dl/`** (ต้องดูแลลิงก์+สรุปการเปลี่ยนแปลงทุกครั้งที่ส่ง)

## P7 · AR มือถือ (ARCore) + Holomap · 3-4 รอบ
วางแมพบนโต๊ะจริง (plane detection + anchor) · ย่อ/ขยาย/หมุนด้วยสัมผัส · holomap ไจโร · แถบควบคุม AR
(เดิม WO-02m — เลื่อนมาหลังปล่อย Play เพราะเป็นของ "ว้าว" ไม่ใช่ของที่ใช้ทุกวัน · โค้ด reuse ต่อ Galaxy XR)

## P8 · Builder แก้ไข (เดิม WO-06 + 08) · 6-8 รอบ
palette+หมวด+variants · วางวัตถุ · gizmo ย้าย/หมุน/สเกล · multi-select+pivot+snap · ก๊อป/ลบ/เปลี่ยนสี/ตั้งชื่อ · รายการวัตถุ+ค้นหา · undo/redo · ประวัติเวอร์ชัน+กู้คืน · save+autosave+ชื่อ/สาธารณะ+thumbnail · **PATCH + rev conflict + editPolicy + สิทธิ์แก้ไขทางอีเมล**
**DoD:** ย้ายวัตถุในแอป → refresh เว็บเห็นตรงกัน และกลับกัน

## P9 · Sculpt + เชือก + env tools (เดิม WO-09) · 5-7 รอบ
แปรง sculpt 6 แบบ (ยก/ขุด/เกลี่ย/noise/สุ่ม/ขนาด-แรง) · เชือก 🪢 (ผูก 2 จุด + catenary + sag/หนา/สี) · ตั้งพื้นที่/ความลาด/ความหนา/ระดับน้ำ
**DoD:** checklist parity ครบ 100% ใน `PARITY.md`

## P10 · Parity QC + ปิด builder เว็บ (เดิม WO-10) · 2-3 รอบ
เปิดแมพจริงทุก template สองฝั่งเทียบภาพ · migrate ผู้ใช้ · เว็บเหลือ viewer/SEO

## PX · Galaxy XR (เดิม WO-02 + UI XR ของ 05) — 🔴 บล็อกด้วยฮาร์ดแวร์
ทำได้ทันทีที่ได้เครื่อง เพราะ `InputRig` ของ P0.5 กับ AR ของ P7 reuse ได้เกือบหมด

---

## ไมล์สโตนที่ user จะเห็น
| # | ได้อะไร | ครอบ | ประมาณ |
|---|---|---|---|
| **M1** | "ดำน้ำเองได้ + ถ่ายรูปได้" | P0, P0.5, P1 | 7-10 รอบ |
| **M2** | "มีเกม + เครื่องมือครบเหมือนเว็บ" | P2, P3 | 5-7 รอบ |
| **M3** | "มีบัญชี + ออฟไลน์ + pins" (ยังเป็น APK sideload) | P4, P5 | 4-5 รอบ |
| **M4** | AR บนโต๊ะ | P7 | 3-4 รอบ |
| **M5** | **สร้าง/แก้แมพในแอปได้เท่าเว็บ** | P8, P9 | 11-15 รอบ |
| **M6** | 🚀 **ขึ้น Play + ปิด builder เว็บ** | P6, P10 | 4-6 รอบ |

รวม ~35-47 รอบ CI · ที่ ~35 นาที/รอบ + เวลาเขียน/ตรวจ = **ประมาณ 8-11 สัปดาห์ทำงานจริง** (ไม่รวมเวลารอ user เทสมือถือ และไม่รวม Galaxy XR)

## ความเสี่ยงที่ต้องตัดสินใจก่อน
- ~~**R1 อัดวิดีโอบนมือถือ**~~ — **ปิดแล้ว: user เลือกไม่ทำใน v1** ใช้ปุ่มอัดหน้าจอของมือถือแทน · ถ่ายรูปยังทำ (MediaStore) · ถ้าจะทำภายหลังคือ native MediaProjection ซึ่งเทสบน VPS ไม่ได้เลย
- **R2 ไม่มี Unity Editor บนเครื่อง** — UI ทุกชิ้นเขียนด้วยโค้ด (จอย/มินิแมพ/HUD) และ 1 รอบ = 35 นาที → งาน UI หนักอย่าง P8 จะกินรอบมาก ควรทำ EditMode test ของ logic ให้มากที่สุดเพื่อลดรอบ
- **R3 กฎ uGUI legacy Text เท่านั้น** (ห้าม TMP/UI Toolkit เพราะ bake SDF font ไม่ได้) → ข้อความไทยทุกที่ต้องใช้ `UiKit.RowHeight()` ไม่งั้นบรรทัดหาย
- **R4 perf บนมือถือจริงยังไม่เคยวัด** — llvmpipe บน CI วัดได้แค่เชิงเปรียบเทียบ · ก่อน P6 ต้องมีรอบ "วัด fps บนเครื่อง user" จริง
- **R5 เกม/บัญชี ผูกกับ API ที่มีอยู่แล้ว** (`/api/wallet`, `/api/account/email/*`, `/api/dive-sites/favorites|media`) — ไม่ต้องแก้ backend แต่ต้องรักษา rev/สิทธิ์ให้ตรงกับเว็บ
